import { Concept } from '../types';

export const SECURITY_CONCEPTS: Concept[] = [
  // ── Part 1 ──────────────────────────────────────────────────────────────────
  {
    id: 'auth-oauth2',
    cat: 'security',
    color: '#ef4444',
    icon: '🔐',
    title: 'OAuth 2.0 & OpenID Connect (OIDC)',
    tag: 'Delegated authorization + identity federation — how Google Login works',
    overview: 'OAuth 2.0 is a delegated authorization protocol — it lets users grant third-party apps scoped access to their resources without sharing passwords. OpenID Connect (OIDC) adds an identity layer on top: it tells the app WHO the user is via a signed JWT called an ID token. Together they power every "Login with Google/GitHub/Apple" flow in modern applications.',
    components: [
      {
        name: 'Authorization Server',
        icon: '🏛️',
        role: 'Issues access, refresh, and ID tokens after authenticating the user',
        detail: 'The trusted identity authority — examples include Auth0, AWS Cognito, Google Identity, Okta. It owns the login UI, verifies credentials, enforces MFA, and signs tokens with its private key. All other parties trust it implicitly.',
      },
      {
        name: 'Resource Server',
        icon: '🗄️',
        role: 'Validates access tokens on every API call, returns 401 if invalid or expired',
        detail: 'Your API or microservice. It never trusts the client directly — it verifies the Bearer token signature using the auth server public key (JWKS endpoint). If the token is expired, tampered, or missing required scopes, it returns 401 Unauthorized.',
      },
      {
        name: 'Client',
        icon: '💻',
        role: 'The app requesting tokens on behalf of the user — web, mobile, or CLI',
        detail: 'Can be confidential (server-side app with a secure client_secret) or public (SPA, mobile app — cannot store secrets). Public clients MUST use PKCE. The client initiates the auth flow, receives tokens, and attaches them to API calls.',
      },
      {
        name: 'Access Token',
        icon: '🎫',
        role: 'Short-lived JWT or opaque token sent as Bearer in Authorization header',
        detail: 'Typically expires in 15 minutes. Contains the user sub, issued-at (iat), expiry (exp), and granted scopes. Resource servers validate it on every request. Short lifetime limits damage if leaked — the window of exploitation is narrow.',
      },
      {
        name: 'Refresh Token',
        icon: '🔄',
        role: 'Long-lived token used to get new access tokens without re-authentication',
        detail: 'Lives hours to days. Stored server-side (never in localStorage). When the access token expires, the client silently sends the refresh token to the token endpoint and receives a new access token. Refresh token rotation: each use issues a new refresh token and invalidates the old one — if the old token is used again, the server detects theft and revokes the entire family.',
      },
    ],
    howItWorks: 'Authorization Code + PKCE flow (most secure — for all public clients): 1. User clicks Login with Google. 2. App generates code_verifier (random 32 bytes, base64url) and code_challenge = BASE64URL(SHA256(code_verifier)). 3. App redirects to auth server with: client_id, redirect_uri, scope=openid profile email, response_type=code, state (CSRF token), code_challenge, code_challenge_method=S256. 4. User authenticates at Google. 5. Google redirects back to redirect_uri with authorization_code. 6. App sends code + code_verifier to auth server token endpoint (server-to-server, never in browser). 7. Auth server verifies SHA256(code_verifier) == code_challenge, issues access_token + refresh_token + id_token. 8. App uses access_token as Bearer header on API calls. 9. When expired, use refresh_token to silently get new access_token. Why PKCE: SPAs and mobile apps are public clients — they cannot store a client_secret. Without PKCE, a stolen authorization code can be exchanged for tokens by an attacker. PKCE binds the code to the session: the attacker has the code but not the code_verifier.',
    decision: {
      choose: [
        'Third-party identity (Login with Google/GitHub/Apple)',
        'Cross-service authorization in microservices',
        'Mobile apps needing secure auth without storing passwords',
        'Enterprise SSO — federate with corporate IdP (Okta, Azure AD)',
      ],
      avoid: [
        'Simple single-service APIs where API keys suffice',
        'Machine-to-machine calls — use Client Credentials flow instead',
        'When you control both client and server with no third-party identity',
      ],
      vs: [
        {
          name: 'Client Credentials',
          when: 'M2M auth — no user involved. Service A authenticates to Service B with client_id + client_secret.',
        },
        {
          name: 'Device Flow',
          when: 'CLI tools, smart TVs — devices without a browser. User enters a code on their phone.',
        },
        {
          name: 'Auth Code + PKCE',
          when: 'All user-facing apps. SPAs, mobile, web. The safe default.',
        },
      ],
    },
    failures: [
      {
        name: 'Missing PKCE on public clients',
        cause: 'Implicit Flow or Auth Code without PKCE in SPAs',
        symptom: 'Stolen auth code exchanged for tokens by an attacker who intercepted the redirect',
        fix: 'Always use PKCE. Implicit Flow is deprecated (RFC 9700). Never use response_type=token in SPAs.',
        severity: 'critical',
      },
      {
        name: 'Tokens in localStorage',
        cause: 'Developer stored JWT in localStorage for convenience',
        symptom: 'XSS attack reads localStorage, steals token — attacker has full API access until expiry',
        fix: 'Use httpOnly, Secure, SameSite=Strict cookies. In-memory for SPAs. Never localStorage.',
        severity: 'critical',
      },
      {
        name: 'Missing state parameter',
        cause: 'No CSRF token included in the authorization request',
        symptom: 'CSRF attack forces a user to link an attacker-controlled account to their profile',
        fix: 'Always include a cryptographically random state param; verify it matches exactly on callback.',
        severity: 'high',
      },
    ],
    a: {
      v: 'Hotel Key Card System',
      t: 'OAuth is like telling the hotel front desk to give the concierge a key card that only opens the gym — not your room. You never hand the concierge your master key (password). The key card (access token) has limited scope and expires.',
      tx: 'OAuth separates "who you are" (authentication, handled by OIDC) from "what you can do" (authorization, handled by OAuth scopes). The app never sees your password.',
      s: 'User → AuthServer → code → App → tokens → ResourceServer',
    },
    te: {
      def: 'OAuth 2.0 is a delegated authorization framework (RFC 6749). OIDC (OpenID Connect) is an identity layer on top of OAuth 2.0 that adds authentication via a signed JWT ID token containing user claims.',
      types: [
        { n: 'Authorization Code + PKCE', d: 'User-facing apps. Most secure. Code exchanged server-side; PKCE prevents code interception.' },
        { n: 'Client Credentials', d: 'M2M auth. No user context. Service authenticates with client_id + client_secret.' },
        { n: 'Device Flow', d: 'Browserless devices (CLI, TV). User authorizes on a secondary device using a short code.' },
        { n: 'Refresh Token Rotation', d: 'Each refresh token use issues a new token and invalidates the old — detects theft via reuse detection.' },
      ],
      when: 'Use OAuth2 whenever users authenticate via third-party IdP or when delegating access across services. Use OIDC when you need user identity (who they are). Use Client Credentials for service-to-service.',
      trade: 'Adds complexity and external IdP dependency. Auth server outage = no logins. Mitigate with managed services (Auth0, Cognito, Clerk). Token revocation is hard with short-lived JWTs — use refresh token revocation as the control point.',
      code: 'const crypto = require(\'crypto\');\n\n// Step 1: Generate PKCE code_verifier and code_challenge\nconst code_verifier = crypto.randomBytes(32).toString(\'base64url\');\nconst code_challenge = crypto\n  .createHash(\'sha256\')\n  .update(code_verifier)\n  .digest(\'base64url\');\n\n// Step 2: Build authorization URL (redirect user here)\nconst authUrl = new URL(\'https://accounts.google.com/o/oauth2/v2/auth\');\nauthUrl.searchParams.set(\'client_id\', process.env.GOOGLE_CLIENT_ID);\nauthUrl.searchParams.set(\'redirect_uri\', \'https://app.example.com/callback\');\nauthUrl.searchParams.set(\'response_type\', \'code\');\nauthUrl.searchParams.set(\'scope\', \'openid profile email\');\nauthUrl.searchParams.set(\'state\', crypto.randomBytes(16).toString(\'hex\'));\nauthUrl.searchParams.set(\'code_challenge\', code_challenge);\nauthUrl.searchParams.set(\'code_challenge_method\', \'S256\');\n\n// Step 3: Exchange authorization code for tokens (server-side)\nasync function exchangeCode(code, code_verifier) {\n  const params = new URLSearchParams({\n    grant_type: \'authorization_code\',\n    client_id: process.env.GOOGLE_CLIENT_ID,\n    client_secret: process.env.GOOGLE_CLIENT_SECRET,\n    redirect_uri: \'https://app.example.com/callback\',\n    code,\n    code_verifier, // Server verifies SHA256(code_verifier) === stored code_challenge\n  });\n\n  const res = await fetch(\'https://oauth2.googleapis.com/token\', {\n    method: \'POST\',\n    headers: { \'Content-Type\': \'application/x-www-form-urlencoded\' },\n    body: params,\n  });\n\n  const { access_token, refresh_token, id_token } = await res.json();\n  return { access_token, refresh_token, id_token };\n}',
      rw: {
        ex: ['Google OAuth', 'GitHub Apps', 'Auth0', 'AWS Cognito', 'Clerk', 'Stripe Connect'],
        cs: 'Stripe Connect uses OAuth 2.0 to let platforms (Shopify, WooCommerce) collect payments on behalf of merchants. The merchant authorizes Stripe to share their account — scoped tokens allow charging customers but not withdrawing to different bank accounts. The access token has explicit scopes that prevent privilege escalation.',
      },
    },
    interview: {
      q: 'Explain Authorization Code + PKCE. Why is PKCE essential for SPAs and what attack does it prevent?',
      a: 'Auth Code + PKCE flow: App generates random code_verifier, computes code_challenge = BASE64URL(SHA256(code_verifier)), sends challenge to auth server in authorization request. Auth server redirects back with one-time code. App exchanges code + original code_verifier for tokens — server-to-server. Auth server re-computes SHA256(code_verifier) and verifies it matches the stored challenge. PKCE prevents authorization code interception attacks: if an attacker intercepts the code (via redirect hijack, browser history, or malicious app on mobile), they cannot exchange it without the code_verifier which never left the originating app. SPAs cannot store client_secret (it would be public in browser bundle), making PKCE the only secure mechanism for public clients.',
      fu: [
        'What is the difference between OAuth 2.0 and OIDC?',
        'Where should access tokens and refresh tokens be stored in a browser app?',
        'How does refresh token rotation detect token theft?',
        'What is the difference between scope and claims in OAuth?',
      ],
    },
  },
  {
    id: 'auth-jwt',
    cat: 'security',
    color: '#ef4444',
    icon: '🎟️',
    title: 'JWT vs Session Tokens',
    tag: 'Stateless vs stateful authentication — the trade-off every architect must know',
    overview: 'JWT (JSON Web Token) is a self-contained, cryptographically signed token — the server validates it locally with no database lookup. Session tokens are opaque random IDs referencing server-side state in Redis or a database. The choice between them determines your scalability, logout capability, and security posture.',
    components: [
      {
        name: 'JWT Header',
        icon: '📋',
        role: 'Declares the signing algorithm and token type',
        detail: 'Base64url-encoded JSON: { "alg": "RS256", "typ": "JWT" }. NEVER trust alg from an incoming token — always specify the expected algorithm in your verify() call. The alg:none attack exploits libraries that read the alg field from the token itself and skip signature verification.',
      },
      {
        name: 'JWT Payload',
        icon: '📦',
        role: 'Contains claims — sub, iat, exp, roles — base64url encoded, NOT encrypted',
        detail: 'Anyone can decode the payload with atob(). Do not store sensitive data (SSN, PII, secrets) in a JWT payload unless using JWE (JSON Web Encryption). Standard claims: sub (subject/user ID), iat (issued at), exp (expiry Unix timestamp), jti (unique token ID for revocation). Custom claims: roles, permissions, tenant.',
      },
      {
        name: 'JWT Signature',
        icon: '✍️',
        role: 'Cryptographic proof the token was issued by your server and was not tampered with',
        detail: 'HMAC-SHA256 (HS256) uses a shared secret — both issuer and verifier must have the same secret. RS256 uses RSA private key to sign and public key to verify — better for microservices (each service only needs the public key, not the secret). ECDSA (ES256) is faster than RSA with equivalent security.',
      },
      {
        name: 'Session ID',
        icon: '🪪',
        role: 'Random UUID stored in httpOnly cookie — opaque reference to server-side state',
        detail: 'Typically 128–256 bits of cryptographic randomness. Stored in an httpOnly, Secure, SameSite=Strict cookie — inaccessible to JavaScript, transmitted only over HTTPS, not sent cross-site. The ID itself reveals nothing about the user; all user data lives in the server-side session store.',
      },
      {
        name: 'Session Store',
        icon: '🗄️',
        role: 'Redis preferred — sub-millisecond lookup, instant revocation by deleting the record',
        detail: 'Redis is the canonical choice: O(1) GET on session_id, TTL-based auto-expiry, atomic operations. Enables instant revocation (DEL session:{id}), multi-device management (one key per device), and session metadata storage. Add Redis Sentinel or Cluster for HA — session store outage = all users logged out.',
      },
    ],
    howItWorks: 'JWT: 1. User logs in. 2. Server creates JWT: header.payload.signature — signs with private key (RS256) or shared secret (HS256). 3. Client stores JWT in httpOnly cookie or memory (NOT localStorage). 4. Every request: client sends JWT in Authorization: Bearer header. 5. Server verifies signature locally — no DB call, O(1). 6. Token expires (15 min). Client uses refresh token to get new access token silently. Cannot revoke before expiry without a blocklist. Session: 1. User logs in. 2. Server creates random session_id, stores {userId, roles, metadata} in Redis with TTL. 3. Sends session_id in httpOnly Secure SameSite=Strict cookie. 4. Every request: server reads session_id from cookie, looks up Redis (sub-ms). 5. Logout: DEL session:{id} from Redis — instant, complete revocation. 6. Multi-device: each device has its own session key; invalidate individually or all at once.',
    decision: {
      choose: [
        'JWT: Stateless microservices — each service validates tokens independently without shared state',
        'JWT: Third-party API consumers where you cannot rely on a shared session store',
        'Sessions: Banking/healthcare apps requiring instant logout and multi-device session management',
        'Sessions: Admin panels where a compromised account must be locked out immediately',
      ],
      avoid: [
        'JWT with long expiry (>1 hour) without a revocation mechanism',
        'Storing sensitive data in JWT payload — it is base64, not encrypted; use JWE if needed',
        'Sessions when services are geographically distributed with no shared Redis',
      ],
      vs: [
        {
          name: 'JWT RS256',
          when: 'Microservices: auth server signs with private key, each service validates with public key — no shared secret.',
        },
        {
          name: 'JWT HS256',
          when: 'Simpler single-service setup. Shared secret. Riskier if secret leaks.',
        },
        {
          name: 'Opaque Session',
          when: 'Web apps needing instant revocation. Redis lookup adds ~1ms but enables full session control.',
        },
      ],
    },
    failures: [
      {
        name: 'JWT in localStorage',
        cause: 'Developer stored JWT in localStorage for convenience or to avoid cookie complexity',
        symptom: 'XSS attack injects a script that reads localStorage and exfiltrates the token to an attacker-controlled server',
        fix: 'Store tokens in httpOnly cookies (inaccessible to JS) or in-memory only. Never localStorage or sessionStorage.',
        severity: 'critical',
      },
      {
        name: 'alg:none attack',
        cause: 'JWT library trusts the alg field from the incoming token header instead of enforcing the expected algorithm',
        symptom: 'Attacker strips the signature, sets alg:none in the header, and the server accepts the unsigned token as valid',
        fix: 'Always pass the expected algorithm explicitly to verify() — never allow the library to auto-detect it from the token. Pin to RS256 or HS256.',
        severity: 'critical',
      },
      {
        name: 'No exp claim',
        cause: 'Developer forgot to set token expiry, or set it to an unreasonably large value',
        symptom: 'A leaked or stolen token remains valid indefinitely — no time-bound on the attack window',
        fix: 'Always set exp. Use short-lived access tokens (15 min) paired with refresh tokens. Treat missing exp as invalid.',
        severity: 'critical',
      },
      {
        name: 'Weak HS256 secret',
        cause: 'Short, predictable, or hardcoded HMAC secret used to sign tokens',
        symptom: 'Attacker collects a JWT, runs offline brute-force against the HMAC secret, forges tokens for any user ID',
        fix: 'Use RS256 (asymmetric — no shared secret to steal) or generate a 256-bit cryptographically random secret for HS256. Rotate regularly.',
        severity: 'critical',
      },
    ],
    a: {
      v: 'Passport vs Library Card',
      t: 'A JWT is a passport — self-contained, verified by anyone with the public key, works across borders (microservices), but if stolen, works until expiry. A session token is a library card — just a reference number; the library (server) holds all info and can cancel the card instantly.',
      tx: 'Passports are great for international travel (distributed systems). Library cards are great for instant revocation when a card is reported stolen.',
      s: 'JWT: verify signature → no DB. Session: lookup Redis → get user state',
    },
    te: {
      def: 'JWT (RFC 7519) is a compact, URL-safe, cryptographically signed token encoding JSON claims. Sessions are server-side state records referenced by an opaque ID in a cookie. The core trade-off: JWT = stateless + scalable + no revocation; Sessions = stateful + revocable + Redis dependency.',
      types: [
        { n: 'RS256 JWT', d: 'Asymmetric. Auth server signs with private key; resource servers verify with public key. No shared secret exposure.' },
        { n: 'HS256 JWT', d: 'Symmetric shared secret. Simpler but riskier — any service that can verify can also forge tokens.' },
        { n: 'Opaque Session', d: 'Random ID maps to server-side state in Redis. Instant revocation. 1ms lookup overhead.' },
        { n: 'Sliding Session', d: 'Session TTL resets on each request — user stays logged in while active. Risk: sessions never expire for active attackers.' },
      ],
      when: 'JWT for stateless APIs, microservices, mobile. Sessions for web apps requiring instant logout, device management, or regulatory compliance (HIPAA, PCI DSS mandate session control).',
      trade: 'JWT: cannot revoke without blocklist (Redis check per request = same as sessions). Payload bloat on every request. Clock skew between servers causes exp validation issues (use nbf + leeway). Sessions: Redis is a critical dependency; add replica + sentinel. Cross-datacenter session reads add latency.',
      code: 'const jwt = require(\'jsonwebtoken\');\nconst fs = require(\'fs\');\n\nconst privateKey = fs.readFileSync(\'private.pem\');\nconst publicKey = fs.readFileSync(\'public.pem\');\n\n// Sign a JWT with RS256 — private key stays on auth server only\nfunction issueToken(userId, roles) {\n  return jwt.sign(\n    { sub: userId, roles, jti: crypto.randomUUID() },\n    privateKey,\n    { algorithm: \'RS256\', expiresIn: \'15m\', issuer: \'auth.example.com\' }\n  );\n}\n\n// Verify — resource server only needs the PUBLIC key\n// ALWAYS specify algorithms — never let the library read alg from the token\nfunction verifyToken(token) {\n  return jwt.verify(token, publicKey, {\n    algorithms: [\'RS256\'], // Explicit allowlist — prevents alg:none attack\n    issuer: \'auth.example.com\',\n  });\n}\n\n// The alg:none attack — what happens without algorithm pinning:\n// Attacker crafts: header={alg:none} + payload={sub:admin} + empty signature\n// Vulnerable library: reads alg from token, skips verification, accepts it\n// Safe library with algorithms:[\'RS256\']: rejects — alg:none not in allowlist\n\n// Token that would be crafted by attacker:\nconst maliciousHeader = Buffer.from(JSON.stringify({ alg: \'none\', typ: \'JWT\' })).toString(\'base64url\');\nconst maliciousPayload = Buffer.from(JSON.stringify({ sub: \'admin\', roles: [\'superadmin\'] })).toString(\'base64url\');\nconst maliciousToken = maliciousHeader + \'.\' + maliciousPayload + \'.\'; // No signature\n// verifyToken(maliciousToken) throws: "invalid algorithm" — protected.',
      rw: {
        ex: ['Auth0 (JWTs)', 'Firebase Auth (JWTs)', 'GitHub web sessions (opaque)', 'Django (sessions)', 'Rails (sessions)', 'Next-Auth (configurable)'],
        cs: 'GitHub uses opaque session tokens (not JWTs) for web sessions — this lets them instantly invalidate sessions when a leaked token is reported via HackerOne. However GitHub Actions workflow tokens are JWTs: short-lived (job duration), scoped to exactly the permissions declared in the workflow yaml, and signed by GitHub. Two different tools for two different problems.',
      },
    },
    interview: {
      q: 'Your banking app uses JWTs with 24-hour expiry. A user reports their phone stolen. How do you invalidate their token immediately?',
      a: 'Pure stateless JWTs cannot be revoked before expiry — this is a fundamental design limitation. Solutions in order of preference: 1. Keep JWT expiry very short (15 min) + use refresh tokens stored server-side. Revoke the refresh token in Redis — the user cannot get new access tokens after their current one expires in 15 min. 2. JWT blocklist: store the jti (JWT ID) in Redis with TTL matching token lifetime. Every validation checks the blocklist. This adds one Redis lookup per request — effectively making JWTs stateful, which defeats their purpose for a single service. 3. For a banking app specifically: use sessions. The 1ms Redis lookup per request is trivially cheap; instant revocation is non-negotiable. The right architecture: short-lived JWTs for API calls between microservices (inter-service auth), sessions for user-facing web sessions (revocability).',
      fu: [
        'What is the alg:none JWT vulnerability and how do you prevent it?',
        'Why is localStorage dangerous for token storage?',
        'How does refresh token rotation detect if a refresh token was stolen?',
        'How do you handle clock skew when validating JWT expiry across distributed servers?',
      ],
    },
  },
  // ── Part 2 ──────────────────────────────────────────────────────────────────
  {
    id: 'auth-tls',
    cat: 'security',
    color: '#ef4444',
    icon: '🔒',
    title: 'TLS & HTTPS Deep Dive',
    tag: 'How the padlock in your browser works — TLS handshake, certificates, and mTLS',
    overview: 'TLS (Transport Layer Security) is the cryptographic protocol securing every HTTPS connection. It provides confidentiality (encrypted data), integrity (tamper detection), and server authentication (certificate verification). Understanding TLS deeply is essential for architects — it affects latency budgets, certificate lifecycle management, mTLS between microservices, and your overall security posture.',
    components: [
      {
        name: 'Certificate Authority',
        icon: '🏛️',
        role: 'Issues and signs X.509 certificates',
        detail: 'Browser trust stores contain ~150 root CAs. Let Encrypt signs 3M+ certs/day free. CAs form a chain of trust: root CA signs intermediate CA, intermediate signs leaf cert. Browsers verify the entire chain.',
      },
      {
        name: 'X.509 Certificate',
        icon: '📜',
        role: 'Contains server identity and public key',
        detail: 'Contains server domain in Subject Alternative Name (SAN), public key, CA digital signature, and validity period (max 398 days since 2020). The CA signature is verified using the CA public key embedded in the browser trust store.',
      },
      {
        name: 'TLS Handshake',
        icon: '🤝',
        role: 'Negotiates cipher suite and derives session keys',
        detail: 'Authenticates server via certificate, negotiates encryption algorithm (cipher suite), and derives shared session keys via ECDHE — all in 1 round trip (TLS 1.3). TLS 1.2 required 2 round trips.',
      },
      {
        name: 'ECDHE (Ephemeral Diffie-Hellman)',
        icon: '🔑',
        role: 'Key exchange enabling Perfect Forward Secrecy',
        detail: 'Both sides compute the same session key without transmitting it. Ephemeral key pairs are generated per session and discarded immediately after the handshake. Even if the server private key is stolen years later, past sessions cannot be decrypted.',
      },
      {
        name: 'mTLS (Mutual TLS)',
        icon: '🔐',
        role: 'Bidirectional certificate authentication',
        detail: 'Both client AND server present certificates. Standard for service-to-service auth in microservices. Istio automatically issues SPIFFE-format certificates to every pod — providing service identity, not user identity. Envoy sidecar proxies handle the TLS transparently.',
      },
    ],
    howItWorks: 'TLS 1.3 Handshake (1-RTT):\n\n1. Client sends ClientHello: max TLS version (1.3), supported cipher suites, random nonce, key_share (ECDH public key).\n\n2. Server sends ServerHello: chosen cipher (TLS_AES_256_GCM_SHA384), its ECDH public key, certificate chain.\n\n3. Both sides independently compute the same session key from ECDH:\n   session_key = ECDH(client_private, server_public) = ECDH(server_private, client_public)\n   Key is never transmitted.\n\n4. Server sends Finished (encrypted with session key — proves it holds the private key matching the certificate).\n\n5. Client verifies certificate chain: CA signature valid? Hostname matches SAN? Not expired? Not revoked (OCSP)?\n\n6. Client sends Finished.\n\n7. Encrypted application data flows.\n\nPerfect Forward Secrecy: ECDHE uses ephemeral key pairs — new ones per session, discarded immediately. Even if the server private key is stolen 5 years later, recorded past traffic cannot be decrypted.\n\nTLS 1.2 weakness: supported RSA key exchange (static — no PFS) and weak cipher suites (RC4, 3DES). TLS 1.3 removes all of these.\n\nmTLS: same process, but client also sends its certificate in step 2. Server verifies client cert against a trusted CA. Used by Istio to auto-issue SPIFFE-format certs to every pod — service identity, not user identity.',
    failures: [
      {
        name: 'Expired Certificate',
        cause: 'No automated renewal — cert expires after 1 year',
        symptom: 'Users see security error, site is completely inaccessible',
        fix: 'Use cert-manager in K8s (auto-renews Let Encrypt certs), AWS ACM (auto-renews), set monitoring alerts 30 days before expiry',
        severity: 'critical',
      },
      {
        name: 'Certificate for Wrong Domain',
        cause: 'Cert issued for api.example.com but site is www.example.com, or missing wildcard coverage',
        symptom: 'Browser rejects connection with hostname mismatch error',
        fix: 'Use Subject Alternative Names (SAN) covering all domains; wildcard *.example.com for subdomains',
        severity: 'high',
      },
      {
        name: 'Weak Cipher Suites Enabled',
        cause: 'Server still supports TLS 1.0, TLS 1.1, or RC4 cipher',
        symptom: 'BEAST, POODLE, SWEET32 attacks possible; PCI DSS non-compliance',
        fix: 'Enforce TLS 1.2 minimum (TLS 1.3 preferred), disable SSLv3/TLS 1.0/1.1, verify with ssllabs.com',
        severity: 'high',
      },
    ],
    decision: {
      choose: [
        'HTTPS everywhere — no exceptions, including internal APIs',
        'mTLS for service-to-service auth in microservices (let Istio/Envoy manage certificates automatically)',
        'TLS termination at load balancer to offload crypto from app servers',
        'Certificate pinning only for high-security mobile apps (careful: pinning breaks on cert rotation)',
      ],
      avoid: [
        'TLS 1.0/1.1 — deprecated, vulnerable to BEAST/POODLE',
        'Self-signed certs in production without a private CA (use Let Encrypt or ACM)',
        'Long-lived TLS session resumption without rotation (reduces forward secrecy benefit)',
      ],
      vs: [
        {
          name: 'TLS Termination at LB',
          when: 'Offload TLS from app servers. Internal traffic plain HTTP (acceptable if network is trusted and mTLS handles service auth).',
        },
        {
          name: 'End-to-End TLS',
          when: 'Compliance requirements (HIPAA, PCI DSS) mandate encryption in transit everywhere, including internal hops.',
        },
      ],
    },
    a: {
      v: 'Sealed Letter with Wax Seal',
      t: 'TLS is like sending a sealed letter through the post. The wax seal (certificate) proves it is from the real sender. The envelope (encryption) means only the recipient can read it. And a special invisible ink (ECDHE) means that even if someone stole the seal stamp later, they cannot read letters sent before.',
      tx: 'The genius of TLS is that encryption keys are negotiated without being transmitted — both parties independently compute the same key from public information (ECDH math). The key exchange is secure even over a wiretapped connection.',
      s: 'ClientHello → ServerHello + Cert → Verify CA → ECDHE key → AES-256-GCM data',
    },
    te: {
      def: 'TLS (Transport Layer Security) is a cryptographic protocol providing confidentiality (AES-256-GCM encryption), integrity (AEAD authentication tag), and server authentication (X.509 certificate verified against trusted CAs) over TCP connections.',
      types: [
        { n: 'TLS 1.3', d: 'Current standard — 1-RTT handshake, mandatory ECDHE for all key exchange, all weak ciphers removed, 0-RTT for repeat clients.' },
        { n: 'mTLS (Mutual TLS)', d: 'Both client and server present certificates — service mesh standard. Istio and Envoy automate cert issuance and rotation per pod.' },
        { n: 'TLS Termination', d: 'Load balancer decrypts TLS and forwards plain HTTP internally — reduces backend CPU load. Acceptable when internal network is trusted.' },
        { n: 'Certificate Pinning', d: 'Embed expected cert fingerprint in client app — prevents rogue CA attacks. Breaks on cert rotation — use with extreme care and a pin backup.' },
      ],
      when: 'TLS everywhere with no exceptions. Automate certificate issuance and renewal. Use mTLS for inter-service auth. Monitor cert expiry proactively. Test TLS configuration with ssllabs.com or testssl.sh.',
      trade: 'TLS 1.3 handshake adds ~1ms latency (mitigated by session resumption, HTTP/2 multiplexing, 0-RTT for repeat clients). mTLS adds certificate rotation complexity — automate with cert-manager or Istio CA. TLS CPU overhead is negligible on modern hardware with AES-NI hardware acceleration.',
      code: '# nginx: enforce TLS 1.3, HSTS, strong ciphers\nserver {\n  listen 443 ssl;\n  ssl_protocols TLSv1.3;          # TLS 1.3 only\n  ssl_prefer_server_ciphers off;  # let client pick from our list\n  ssl_ciphers TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256;\n  ssl_certificate     /etc/ssl/certs/example.com.crt;\n  ssl_certificate_key /etc/ssl/private/example.com.key;\n\n  # HSTS: tell browsers to use HTTPS for 1 year\n  add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload";\n}\n\n# Inspect certificate expiry date\nopenssl s_client -connect example.com:443 -servername example.com < /dev/null 2>/dev/null \\\n  | openssl x509 -noout -dates\n# notBefore=Jan  1 00:00:00 2025 GMT\n# notAfter =Mar 31 23:59:59 2026 GMT',
      rw: {
        ex: [
          'Cloudflare (TLS termination at 300+ PoPs)',
          'Let Encrypt (90-day auto-renewing certs)',
          'Istio (mTLS between all pods)',
          'AWS ACM (managed certs + auto-renewal)',
          'cert-manager (K8s TLS automation)',
        ],
        cs: 'Cloudflare terminates TLS at 300+ edge PoPs worldwide, then re-encrypts traffic to the origin using a private CA. Origins only need to trust Cloudflare CA — they are not exposed to the public internet. Cloudflare also supports mTLS for API zero-trust: only clients presenting a valid Cloudflare-issued client certificate can reach the origin. This eliminates credential-based attacks on APIs entirely.',
      },
    },
    interview: {
      q: 'Explain the TLS 1.3 handshake. What is Perfect Forward Secrecy and why does it matter practically?',
      a: 'TLS 1.3 handshake in one round trip: client sends ClientHello with its ECDH public key; server responds with ServerHello (chosen cipher), its ECDH public key, and its certificate chain. Both parties independently compute the same session key using the ECDH math — the key is never transmitted. Server sends Finished encrypted with this key, proving it holds the private key matching the certificate. Client verifies the certificate chain against its trusted CA store (signature valid, hostname matches SAN, not expired, not revoked via OCSP). Session is now encrypted with AES-256-GCM. Perfect Forward Secrecy: ECDHE uses ephemeral key pairs — a new pair is generated for every session and immediately discarded after the handshake. This means: if an attacker records all encrypted traffic today and later steals the server private key (breach, court order, key rotation mistake), they still cannot decrypt the recorded traffic — they would need the ephemeral session key which was never stored. This is the critical difference from older RSA key exchange where all past traffic could be retroactively decrypted with the stolen private key.',
      fu: [
        'What is SNI and why is it needed for virtual hosting?',
        'How does certificate transparency (CT) logging help detect rogue certificates?',
        'What is OCSP stapling and why is it better than live OCSP checks?',
        'How does Istio automatically issue and rotate mTLS certificates for microservices?',
      ],
    },
  },
  {
    id: 'auth-owasp',
    cat: 'security',
    color: '#ef4444',
    icon: '🛡️',
    title: 'OWASP Top 10 Vulnerabilities',
    tag: 'The 10 most critical web security risks — every Principal Engineer must know these cold',
    overview: 'The OWASP Top 10 (2021 edition) is the authoritative reference for web application security risks. 94% of applications tested have some form of broken access control. Understanding these vulnerabilities — how they are exploited, detected, and prevented — is a baseline expectation for any engineer at Principal level or above.',
    components: [
      {
        name: 'A01 Broken Access Control',
        icon: '🚪',
        role: 'Most common — 94% of apps affected',
        detail: 'Horizontal privilege escalation (IDOR/BOLA — accessing another user data by changing an ID) and vertical privilege escalation (BFLA — accessing higher-privilege functions). Fix: resource-level ownership check on every endpoint, not just route-level auth middleware.',
      },
      {
        name: 'A03 Injection',
        icon: '💉',
        role: 'Unsanitized input injected into interpreters',
        detail: 'SQL, NoSQL, command, and LDAP injection via user input concatenated into queries or commands. Attacker can dump databases, bypass authentication, or execute OS commands. Fix: parameterized queries always, never string-concatenate user input into queries.',
      },
      {
        name: 'A03 XSS (Cross-Site Scripting)',
        icon: '📜',
        role: 'Malicious scripts injected into trusted web pages',
        detail: 'Storing or reflecting unsanitized user input as HTML. Attacker injects script tags that steal session cookies, redirect users, or perform actions on their behalf. Fix: contextual output escaping (React does this by default), Content-Security-Policy header.',
      },
      {
        name: 'A07 Identification & Auth Failures',
        icon: '🔑',
        role: 'Weak authentication and session management',
        detail: 'Weak passwords, missing MFA, no brute force protection, credential stuffing, insecure session tokens. Fix: Argon2id for password hashing, enforce MFA, rate-limit login endpoints, check passwords against breach databases (HaveIBeenPwned API).',
      },
      {
        name: 'A10 SSRF (Server-Side Request Forgery)',
        icon: '🌐',
        role: 'Server fetches attacker-controlled URLs',
        detail: 'Server fetches user-supplied URLs without validation — attacker targets internal services or the cloud metadata endpoint (169.254.169.254) to steal IAM credentials. Fix: allowlist valid external domains, block RFC 1918 private IP ranges in all outbound HTTP requests.',
      },
    ],
    howItWorks: 'Most exploited vulnerabilities in order of real-world impact:\n\nIDOR/BOLA: GET /api/orders/12345 — attacker changes ID to any number, gets another user\'s data because server only checks authentication, not resource ownership.\n\nSQL Injection: SELECT * FROM users WHERE id = \'' + 'userId\' — attacker sends: 1 OR 1=1, dumps entire table or bypasses auth.\n\nXSS Stored: attacker posts a comment with a script tag — every visitor who views the comment has their session cookie sent to the attacker\'s server.\n\nCSRF: attacker tricks a logged-in user into submitting a state-changing request (money transfer) — browser auto-sends cookies with the forged request.\n\nSSRF: image upload feature fetches the URL you provide — attacker provides http://169.254.169.254/latest/meta-data/iam/security-credentials/role — server returns AWS IAM credentials.\n\nMass Assignment: POST /users with {"name":"Alice","isAdmin":true} — if server binds all request fields to the model, user becomes admin.\n\nExcessive Data Exposure: GET /users/profile returns the entire user row including password_hash, SSN, internal_id — server should filter to a response DTO.',
    failures: [
      {
        name: 'SQL Injection',
        cause: 'String-concatenating user input into SQL query strings',
        symptom: 'Attacker dumps entire database, bypasses auth with OR 1=1, deletes data',
        fix: 'Always use parameterized queries (db.query($1, [value])) or ORMs — never concatenate',
        severity: 'critical',
      },
      {
        name: 'IDOR / BOLA',
        cause: 'Endpoint checks authentication but not resource ownership',
        symptom: 'Changing an order ID in the URL returns another user\'s private data',
        fix: 'After fetching: if (resource.userId !== req.user.id) return 403. Check on EVERY data endpoint.',
        severity: 'critical',
      },
      {
        name: 'XSS (Cross-Site Scripting)',
        cause: 'Rendering unsanitized user input as raw HTML in the browser',
        symptom: 'Script tag in user-submitted content executes for all subsequent visitors',
        fix: 'React escapes by default — avoid dangerouslySetInnerHTML. Set Content-Security-Policy header.',
        severity: 'critical',
      },
      {
        name: 'SSRF',
        cause: 'Server fetches user-supplied URLs without domain or IP validation',
        symptom: 'Attacker fetches internal services or AWS metadata endpoint to steal IAM credentials',
        fix: 'Allowlist valid external domains. Block RFC 1918 addresses (10.x, 172.16.x, 192.168.x, 169.254.x) in all outbound requests.',
        severity: 'critical',
      },
      {
        name: 'Mass Assignment',
        cause: 'Spreading entire request body onto model object without field whitelisting',
        symptom: 'Attacker adds isAdmin:true to a profile update request and escalates privilege',
        fix: 'Whitelist accepted fields explicitly. Use DTOs and schema validation (Zod, Pydantic) at every API boundary.',
        severity: 'critical',
      },
    ],
    decision: {
      choose: [
        'Parameterized queries for all database interactions',
        'Resource-level authorization checks on every endpoint (not just route-level auth middleware)',
        'Content-Security-Policy headers to limit XSS blast radius',
        'Input validation at every system boundary using schema validation (Zod, Joi, Pydantic)',
      ],
      avoid: [
        'String concatenation into SQL queries — ever',
        'Trusting client-supplied data for authorization decisions',
        'Returning raw database rows in API responses',
        'Allowing server-side URL fetching without strict domain allowlisting',
      ],
      vs: [
        {
          name: 'SAST (Static Analysis)',
          when: 'Catches injection and XSS patterns at CI time. Tools: Semgrep, SonarQube, CodeQL — runs on every PR.',
        },
        {
          name: 'DAST (Dynamic Analysis)',
          when: 'OWASP ZAP and Burp Suite actively attack a running app to find BOLA, SSRF, and auth issues that static analysis misses.',
        },
      ],
    },
    a: {
      v: 'Home Security Vulnerabilities',
      t: 'SQL injection is leaving your front door open. IDOR is giving every tenant the same master key to every apartment. XSS is letting strangers put hidden cameras in your lobby. SSRF is a delivery person you asked to fetch a package who instead walks into every room in your house. CSRF is someone forging your signature while your hand is on the pen.',
      tx: 'The vast majority of breaches exploit well-known, preventable vulnerabilities — not sophisticated zero-days. Mastering OWASP Top 10 prevents the overwhelming majority of real attacks.',
      s: 'Input → Validate → Parameterize → Authenticate → Authorize per resource → Filter output → Log',
    },
    te: {
      def: 'The OWASP Top 10 is a standard awareness document for the most critical security risks to web applications, updated every 3-4 years based on CVE data and security survey responses from hundreds of organizations.',
      types: [
        { n: 'Injection Flaws', d: 'SQL, command, LDAP injection via unsanitized input — parameterized queries and ORMs prevent these by default.' },
        { n: 'Access Control Failures', d: 'IDOR (horizontal — access peer data) and BFLA (vertical — access admin functions) — resource-level ownership checks fix both.' },
        { n: 'Client-Side Attacks', d: 'XSS (injected scripts) and CSRF (forged requests) — CSP headers and SameSite=Strict cookies are the primary mitigations.' },
        { n: 'Infrastructure Attacks', d: 'SSRF (server fetches internal URLs) and XXE (XML external entity injection) — URL allowlisting and hardened XML parser settings fix these.' },
      ],
      when: 'Apply OWASP controls at design time (threat modeling), implementation time (code review checklists, SAST in CI), and testing time (DAST against staging, penetration testing before launch, bug bounty program in production).',
      trade: 'Security controls add development time and some latency. Automate as much as possible: ORM prevents injection by default, React prevents XSS by default, Dependabot alerts on CVEs, Semgrep runs in CI. The residual manual effort (resource-level auth checks, security code review) cannot be fully automated — build it into PR review culture.',
      code: '// WRONG: SQL injection vulnerability\nconst result = await db.query(\n  `SELECT * FROM orders WHERE id = ${req.params.id}`  // never do this\n);\n\n// RIGHT: Parameterized query\nconst result = await db.query(\n  \'SELECT * FROM orders WHERE id = $1\',\n  [req.params.id]\n);\n\n// IDOR fix: check resource ownership after fetch\nasync function getOrder(req, res) {\n  const order = await db.query(\'SELECT * FROM orders WHERE id = $1\', [req.params.id]);\n  if (!order) return res.status(404).json({ error: \'Not found\' });\n  if (order.userId !== req.user.id) return res.status(403).json({ error: \'Forbidden\' });\n  return res.json(order);\n}\n\n// SSRF fix: validate URL before server-side fetch\nimport { URL } from \'url\';\nconst PRIVATE_IP = /^(10\\.|172\\.(1[6-9]|2\\d|3[01])\\.|192\\.168\\.|169\\.254\\.)/;\n\nfunction isSafeUrl(urlStr) {\n  try {\n    const parsed = new URL(urlStr);\n    if (!["http:", "https:"].includes(parsed.protocol)) return false;\n    if (PRIVATE_IP.test(parsed.hostname)) return false;  // block RFC1918 + metadata\n    return ALLOWLISTED_DOMAINS.has(parsed.hostname);\n  } catch { return false; }\n}',
      rw: {
        ex: [
          'HackerOne (bug bounty platform)',
          'Snyk (dependency vulnerability scanning)',
          'Semgrep (SAST — runs injection pattern checks in CI)',
          'OWASP ZAP (DAST — active scanner)',
          'Burp Suite (penetration testing)',
          'AWS WAF (SSRF and injection protection at edge)',
        ],
        cs: 'Capital One breach (2019): 100 million customer records exposed via SSRF. A misconfigured AWS WAF allowed a request to the EC2 Instance Metadata Service (IMDSv1, reachable at 169.254.169.254). The attacker retrieved IAM credentials attached to the WAF instance, which had overly broad S3 access. Prevention: block 169.254.x.x in WAF rules, use IMDSv2 (requires a session token — prevents SSRF from retrieving metadata), apply least-privilege IAM roles. The attacker was caught because CloudTrail logged the unusual S3 ListBuckets calls.',
      },
    },
    interview: {
      q: 'A penetration tester finds that GET /api/invoices/789 returns another user\'s invoice when authenticated as a different user. What is this vulnerability, how did it occur, and what is your remediation strategy across the entire API?',
      a: 'This is IDOR (Insecure Direct Object Reference), classified as OWASP A01 Broken Access Control. It occurs when the server authenticates the request (valid session/token) but does not verify that the authenticated user is authorized to access this specific resource — horizontal privilege escalation. Remediation has four layers: 1. Immediate fix: add a resource ownership check to every endpoint that returns user-specific data — fetch the resource, then verify resource.userId === req.user.id before returning. Return 403 if mismatch. 2. Systemic fix: create a data access layer where all user-scoped queries automatically filter by the authenticated user ID — the query itself is the authorization check: SELECT * FROM invoices WHERE id = $1 AND user_id = $2. This prevents the developer from forgetting the check. 3. Use non-sequential UUIDs or ULIDs as public IDs instead of auto-increment integers. This is defense-in-depth, not a replacement for authorization, but makes enumeration impractical. 4. Write integration tests that authenticate as User A and attempt to read User B resources — expect 403. Run these in CI on every deploy. Add DAST scanning (Burp Suite, OWASP ZAP) in the pre-production pipeline to catch BOLA before it reaches production.',
      fu: [
        'What is the difference between BOLA and BFLA?',
        'How does Content-Security-Policy prevent or limit XSS?',
        'How does SSRF lead to cloud credential theft — walk through the Capital One breach?',
        'What is the difference between authentication and authorization?',
      ],
    },
  },
  // ── Part 3 ──────────────────────────────────────────────────────────────────
  {
    id: 'auth-rbac',
    cat: 'security',
    color: '#ef4444',
    icon: '👥',
    title: 'RBAC & ABAC Access Control',
    tag: 'Role-Based and Attribute-Based Access Control — architecting permissions at scale',
    overview: 'RBAC assigns permissions to roles and roles to users. ABAC makes access decisions based on attributes of the user, resource, and environment. ReBAC (Relationship-Based, Google Zanzibar model) grants access based on the relationship graph between subjects and resources. Choosing the right model is an architectural decision that determines how flexible, auditable, and scalable your permission system will be.',
    components: [
      {
        name: 'Subject',
        icon: '👤',
        role: 'Who is requesting access',
        detail: 'User, service account, or API client requesting access — has attributes: department, clearance, roles',
      },
      {
        name: 'Resource',
        icon: '📄',
        role: 'What is being accessed',
        detail: 'What is being accessed — has attributes: owner, classification, workspace_id',
      },
      {
        name: 'Action',
        icon: '⚡',
        role: 'What operation is requested',
        detail: 'read, write, delete, execute',
      },
      {
        name: 'Policy Engine',
        icon: '⚙️',
        role: 'Evaluates rules and returns allow/deny',
        detail: 'OPA, AWS IAM, Casbin — evaluates rules and returns allow/deny',
      },
      {
        name: 'Role',
        icon: '🏷️',
        role: 'Named bundle of permissions',
        detail: 'RBAC: a named bundle of permissions — admin, editor, viewer — users inherit all permissions of assigned roles',
      },
    ],
    howItWorks: 'RBAC flow: 1. Define permissions (orders:read, orders:write, users:manage). 2. Bundle into roles (admin=all, manager=orders:read+write, viewer=orders:read). 3. Assign roles to users. 4. Middleware checks: does user role include required permission? ABAC flow: 1. Define policy rules in Rego (OPA) or IAM JSON: allow if user.department == resource.department AND action == read AND env.time is business_hours. 2. On each request, collect: user attributes (dept, clearance), resource attributes (owner, classification), environment (time, IP, device). 3. Policy engine evaluates all matching rules → allow or deny. ReBAC (Zanzibar) flow: 1. Store relationship tuples: (user:alice, viewer, doc:report) or (group:finance, editor, folder:q4-reports). 2. On access check: traverse relationship graph — is there a path from alice to doc:report with viewer or higher permission? 3. Handles inheritance: alice is viewer of report because alice is member of finance who is editor of the parent folder. Role explosion problem: with RBAC, you start with 3 roles (admin, editor, viewer), then add roles for each team, region, and data tier — suddenly 50+ roles. Adding a new permission means updating 30 role definitions. Switch to ABAC when you hit ~20 roles.',
    failures: [
      {
        name: 'Role explosion',
        cause: 'RBAC with too many roles — every new requirement adds a role',
        symptom: '50+ roles, adding permission requires modifying dozens of role definitions, audit is impossible',
        fix: 'Decompose into fine-grained permissions + ABAC policies; use OPA for externalized policy logic',
        severity: 'medium',
      },
      {
        name: 'RBAC without resource-level checks',
        cause: 'Middleware checks role but not resource ownership',
        symptom: 'Admin role can see any user data (correct) but manager role also returns all orders not just their team (BOLA)',
        fix: 'Combine RBAC (function-level) with resource-level ownership checks',
        severity: 'critical',
      },
      {
        name: 'Over-privileged service accounts',
        cause: 'Service account given admin role for convenience',
        symptom: 'If service is compromised, attacker has full system access',
        fix: 'Least privilege — each service gets only the permissions it needs for its specific operations',
        severity: 'critical',
      },
    ],
    decision: {
      choose: [
        'RBAC for small apps with <10 well-defined, stable roles',
        'ABAC when rules depend on data attributes (department, data classification, time window, user tier)',
        'ReBAC for collaborative apps with hierarchical resource sharing (Google Docs, Notion, GitHub)',
        'OPA for policy-as-code: version-controlled, testable policies deployed independently of app code',
      ],
      avoid: [
        'RBAC when you have >20 roles — role explosion makes it unmaintainable',
        'Rolling your own ABAC engine — use OPA, Casbin, or Permit.io',
        'Mixing authorization logic in business logic — externalize to a policy layer',
      ],
      vs: [
        {
          name: 'OPA (Open Policy Agent)',
          when: 'Kubernetes admission control, API authorization. Rego policy language. CNCF graduated.',
        },
        {
          name: 'Casbin',
          when: 'Embedded authorization in apps. Supports RBAC, ABAC, ACL. Multiple language SDKs.',
        },
        {
          name: 'AWS IAM',
          when: 'AWS resource access. Attribute-based (resource tags). Deny-by-default. 10MB policy size limit.',
        },
      ],
    },
    a: {
      v: 'Building Access Control System',
      t: 'RBAC is like a building where your badge lets you into the floors assigned to your role (engineer=floors 1-3, manager=1-5, admin=all). ABAC is smarter: your badge + department + the room classification + time of day all factor into the decision. ReBAC is the most flexible: you can enter a room if you or your team has been explicitly granted access to it or any parent folder.',
      tx: 'Start with RBAC — simple and auditable. Add ABAC when your rules get complex. Add ReBAC when resources have hierarchical ownership (user → folder → document).',
      s: 'RBAC: user.role → permissions. ABAC: user + resource + env → policy engine → allow/deny',
    },
    te: {
      def: 'RBAC grants access based on assigned roles (user → role → permissions). ABAC evaluates policies over attributes of user, resource, and environment at runtime. ReBAC checks relationship graphs between subjects and resources — powers Google Zanzibar, used by Google Docs, Drive, YouTube.',
      types: [
        { n: 'Flat RBAC', d: 'Simple role→permission mapping, most SaaS' },
        { n: 'Hierarchical RBAC', d: 'Roles inherit from parent roles' },
        { n: 'ABAC/PBAC', d: 'Policy-Based: dynamic attribute evaluation at request time, AWS IAM' },
        { n: 'ReBAC/Zanzibar', d: 'Relationship graph traversal: user is viewer because they are member of a group that has editor on the parent folder' },
      ],
      when: 'Start with RBAC. Add ABAC when role explosion starts (>15-20 roles) or when access rules depend on data attributes. Add ReBAC when users share resources with hierarchical permissions (folders containing documents, orgs containing repos).',
      trade: 'RBAC: simple to implement and audit; brittle as requirements grow. ABAC: very flexible; policy debugging is hard, performance cost of evaluating complex policies (cache decisions with short TTL). ReBAC: powerful for hierarchical resources; requires a relationship store (SpiceDB, Zanzibar) — operational complexity.',
      code: '// RBAC middleware — role→permission map\nconst PERMISSIONS: Record<string, string[]> = {\n  admin: [\'orders:read\', \'orders:write\', \'users:manage\', \'billing:access\'],\n  manager: [\'orders:read\', \'orders:write\'],\n  viewer: [\'orders:read\'],\n};\n\nfunction rbacMiddleware(requiredPermission: string) {\n  return (req: Request, res: Response, next: NextFunction) => {\n    const userRole = req.user?.role;\n    const allowed = PERMISSIONS[userRole]?.includes(requiredPermission);\n    if (!allowed) return res.status(403).json({ error: \'Forbidden\' });\n    next();\n  };\n}\n\n// ABAC — call OPA policy engine\nasync function abackCheck(\n  user: { department: string; clearance: string },\n  resource: { owner: string; classification: string },\n  action: string\n): Promise<boolean> {\n  const res = await fetch(\'http://opa:8181/v1/data/authz/allow\', {\n    method: \'POST\',\n    headers: { \'Content-Type\': \'application/json\' },\n    body: JSON.stringify({ input: { user, resource, action } }),\n  });\n  const { result } = await res.json();\n  return result === true;\n}',
      rw: {
        ex: [
          'AWS IAM (ABAC with resource tags)',
          'Google Zanzibar (ReBAC — Google Docs permissions)',
          'OPA (Open Policy Agent — Kubernetes, API auth)',
          'Casbin (embedded RBAC/ABAC)',
          'Permit.io (managed permissions)',
          'GitHub (ReBAC: org→repo→branch permissions)',
        ],
        cs: 'Google Zanzibar (published 2019) powers permissions for Google Docs, Drive, Calendar, YouTube, and Maps. It evaluates "can user U perform action A on resource R?" by traversing a distributed relationship graph. A document can have direct viewers, inherited viewers from parent folders, and group-based access — all resolved via tuple traversal. Zanzibar handles 10M+ authorization checks per second with sub-10ms latency at Google scale using a globally consistent, Spanner-backed relationship store.',
      },
    },
    interview: {
      q: 'Your SaaS started with 3 roles: admin, editor, viewer. You now have 47 roles and adding a new permission requires touching 30 role definitions. Diagnose and redesign.',
      a: 'This is textbook role explosion — the core failure mode of RBAC at scale. Root cause: every new access requirement spawned a new role rather than composing existing permissions. Redesign in three steps: 1. Decompose monolithic roles into fine-grained permissions: orders:read, orders:write, reports:view, users:manage, billing:access. Roles become named permission bundles, not the source of truth. 2. Introduce ABAC for contextual rules: IF user.department == resource.workspace_owner AND resource.sensitivity != high THEN allow orders:read. This replaces dozens of department-specific roles with a single policy rule. 3. Externalize policy logic to OPA (Open Policy Agent): policies become Rego files version-controlled alongside code, tested independently, deployed without app changes. Teams can update access rules via PR without touching application code. 4. Add ReBAC for resource-level sharing: documents owned by teams, accessed by members of those teams — relationship tuples replace complex ABAC conditions. End state: ~10 base permissions, 3-5 base roles, ~20 ABAC policy rules covering all edge cases. Adding a new permission is one policy rule, not 30 role updates.',
      fu: [
        'What is Google Zanzibar and how does it solve the distributed authorization consistency problem?',
        'How does AWS IAM implement ABAC using resource tags?',
        'What is the principle of least privilege and how do you enforce it for microservices?',
        'How do you handle authorization caching for high-traffic APIs without serving stale access decisions?',
      ],
    },
  },
  {
    id: 'auth-encryption',
    cat: 'security',
    color: '#ef4444',
    icon: '🔑',
    title: 'Encryption, Hashing & Secrets',
    tag: 'Symmetric, asymmetric, password hashing — and the catastrophic mistakes of confusing them',
    overview: 'Encryption, hashing, and secret management are the three pillars of data security. Confusing them causes catastrophic breaches: encrypting passwords (wrong — should be hashed), hashing data you need to retrieve (wrong — should be encrypted), or using fast hashes for passwords (wrong — trivially brute-forced). Understanding when to use each — and which specific algorithm — is a Principal Engineer baseline.',
    components: [
      {
        name: 'AES-256-GCM',
        icon: '🔐',
        role: 'Symmetric authenticated encryption',
        detail: 'Symmetric encryption — one key encrypts+decrypts, hardware-accelerated, authenticated — detects tampering — gold standard for data at rest',
      },
      {
        name: 'RSA/ECDSA',
        icon: '🗝️',
        role: 'Asymmetric encryption and signing',
        detail: 'Asymmetric — public key encrypts/verifies, private key decrypts/signs — used for TLS key exchange and digital signatures',
      },
      {
        name: 'Argon2id',
        icon: '🛡️',
        role: 'Password hashing — memory-hard',
        detail: 'Password hashing — intentionally slow, memory-hard, resistant to GPU and ASIC attacks — 2015 Password Hashing Competition winner',
      },
      {
        name: 'HMAC-SHA256',
        icon: '✍️',
        role: 'Keyed message authentication',
        detail: 'Keyed hash for message authentication — proves message was signed by holder of the secret key — used in JWT HS256, Stripe webhook signatures',
      },
      {
        name: 'Envelope Encryption',
        icon: '📦',
        role: 'Two-layer key hierarchy',
        detail: 'Two-layer key hierarchy: DEK encrypts data, KEK in KMS encrypts DEK — rotating encryption key means re-encrypting the DEK, not all data',
      },
    ],
    howItWorks: 'When to use what: ENCRYPT when you need the data back in plaintext (credit card numbers, SSNs, health data — AES-256-GCM). HASH when you only need to verify (passwords — Argon2id). SIGN when you need to prove origin without encryption (JWT signature, webhook payloads — HMAC or ECDSA). AES-256-GCM details: symmetric (one key), authenticated encryption (AEAD — the auth tag detects tampering before decryption), requires a unique IV (12 bytes random) per encryption. Store: IV + auth_tag + ciphertext together. Argon2id vs SHA256 for passwords: SHA256 hashes 10 billion passwords/second on a consumer GPU — the entire RockYou password list cracked in seconds. Argon2id with memoryCost=64MB takes 200ms per hash — GPU-parallel cracking requires 64MB RAM per thread, making it 10 million times harder to brute-force. bcrypt is still acceptable (work factor 12 = ~300ms) but Argon2id is preferred for new systems. Envelope encryption: generate random 32-byte DEK per record/bucket. Encrypt data with DEK (AES-256-GCM). Encrypt DEK with KEK stored in AWS KMS (never exported — KMS encrypts/decrypts only). To read: call KMS.Decrypt(encrypted_DEK) → get DEK → decrypt data. Key rotation: re-encrypt DEK with new KEK — no need to re-encrypt all data. Each KMS call is logged in CloudTrail.',
    failures: [
      {
        name: 'SHA256 for passwords',
        cause: 'Developer used SHA256 thinking it is cryptographically secure (it is — but for the wrong use case)',
        symptom: 'GPU brute-forces all 8-char passwords in minutes after DB breach',
        fix: 'Argon2id (memoryCost=65536, timeCost=3) or bcrypt (cost=12)',
        severity: 'critical',
      },
      {
        name: 'Encrypting passwords instead of hashing',
        cause: 'Misunderstanding — thinking reversible is safer',
        symptom: 'Encryption key leak = all passwords recoverable',
        fix: 'Passwords must be hashed (one-way): hash+salt with Argon2id',
        severity: 'critical',
      },
      {
        name: 'AES-ECB mode',
        cause: 'Using ECB (Electronic Codebook) instead of GCM',
        symptom: 'Identical plaintext blocks produce identical ciphertext blocks — patterns visible in data (the famous ECB Linux Penguin)',
        fix: 'Always AES-256-GCM (authenticated) or AES-256-CBC+HMAC',
        severity: 'high',
      },
      {
        name: 'Hardcoded encryption key in source code',
        cause: 'Developer put key in constants file',
        symptom: 'Key committed to GitHub → all encrypted data decryptable',
        fix: 'Store keys in KMS/Vault only, derive per-environment keys, rotate on exposure',
        severity: 'critical',
      },
    ],
    decision: {
      choose: [
        'AES-256-GCM for all data at rest (PII, health data, payment data)',
        'Argon2id for all new password hashing (bcrypt acceptable for existing systems)',
        'HMAC-SHA256 for webhook signatures and JWT HS256',
        'Envelope encryption for database column encryption or file encryption at scale',
      ],
      avoid: [
        'MD5 or SHA1 or SHA256 for passwords — they are fast hashes, not password hashes',
        'AES-ECB mode — identical blocks produce identical ciphertext',
        'Storing encryption keys alongside encrypted data',
      ],
      vs: [
        {
          name: 'Argon2id',
          when: 'New systems. Memory-hard (64MB per hash). Best resistance to GPU and ASIC attacks.',
        },
        {
          name: 'bcrypt',
          when: 'Existing systems already using bcrypt. Work factor 12. Still adequate. Less memory-hard than Argon2id.',
        },
        {
          name: 'scrypt',
          when: 'Alternative to Argon2id. Memory-hard. Less widely supported.',
        },
      ],
    },
    a: {
      v: 'Secure Document Handling',
      t: 'Encryption is a lockbox — you can retrieve the contents with the key. Hashing is a paper shredder — once shredded, you can only verify if a new document would shred to the same pieces. HMAC is a wax seal — it proves who signed it without hiding the content. Use the right tool: shred passwords, lockbox credit cards, seal API messages.',
      tx: 'The most dangerous mistake is encrypting passwords — if the encryption key leaks, all passwords are instantly recoverable. Hashing is one-way by design. The second most dangerous is using a fast hash (SHA256) for passwords — fast for verification means fast for brute-forcing.',
      s: 'Passwords → Argon2id hash. PII → AES-256-GCM encrypt. Messages → HMAC sign.',
    },
    te: {
      def: 'Encryption is reversible transformation using a key (AES-256-GCM). Hashing is an irreversible one-way function (Argon2id). HMAC is a keyed hash for authentication — proving the message was created by the key holder. Each serves a distinct purpose; confusing them creates critical vulnerabilities.',
      types: [
        { n: 'AES-256-GCM', d: 'Symmetric AEAD encryption — data at rest' },
        { n: 'RSA-OAEP/ECDH', d: 'Asymmetric — key exchange, digital signatures' },
        { n: 'Argon2id', d: 'Password hashing — memory-hard, GPU-resistant' },
        { n: 'HMAC-SHA256', d: 'Message authentication — webhook and JWT signing' },
      ],
      when: 'Argon2id for every password. AES-256-GCM for every piece of PII, health data, or payment data stored at rest. TLS for all data in transit. HMAC for any message that needs origin verification. Envelope encryption for key-at-rest patterns.',
      trade: 'Argon2id intentionally adds 200-300ms to login (feature, not bug — makes brute-forcing impractical). AES-256-GCM at the application layer (before DB storage) adds complexity but provides defense-in-depth: even a full database dump is useless without the KMS key. KMS latency: ~5ms per decrypt call — cache decrypted DEKs in memory.',
      code: '// Argon2id password hash + verify (Node.js)\nimport argon2 from \'argon2\';\n\nconst ARGON2_OPTIONS = {\n  type: argon2.argon2id,\n  memoryCost: 65536, // 64MB\n  timeCost: 3,\n  parallelism: 1,\n};\n\nasync function hashPassword(password: string): Promise<string> {\n  return argon2.hash(password, ARGON2_OPTIONS);\n}\n\nasync function verifyPassword(hash: string, password: string): Promise<boolean> {\n  return argon2.verify(hash, password);\n}\n\n// AES-256-GCM encrypt with random IV\nimport { randomBytes, createCipheriv, createDecipheriv } from \'crypto\';\n\nfunction encrypt(plaintext: string, key: Buffer): { iv: string; tag: string; data: string } {\n  const iv = randomBytes(12);\n  const cipher = createCipheriv(\'aes-256-gcm\', key, iv);\n  const encrypted = Buffer.concat([cipher.update(plaintext, \'utf8\'), cipher.final()]);\n  const tag = cipher.getAuthTag();\n  return {\n    iv: iv.toString(\'base64\'),\n    tag: tag.toString(\'base64\'),\n    data: encrypted.toString(\'base64\'),\n  };\n}\n\nfunction decrypt(enc: { iv: string; tag: string; data: string }, key: Buffer): string {\n  const decipher = createDecipheriv(\'aes-256-gcm\', key, Buffer.from(enc.iv, \'base64\'));\n  decipher.setAuthTag(Buffer.from(enc.tag, \'base64\'));\n  return decipher.update(enc.data, \'base64\', \'utf8\') + decipher.final(\'utf8\');\n}\n\n// HMAC webhook verification\nimport { createHmac, timingSafeEqual } from \'crypto\';\n\nfunction verifyWebhookSignature(payload: string, signature: string, secret: string): boolean {\n  const expected = createHmac(\'sha256\', secret).update(payload).digest(\'hex\');\n  const expectedBuf = Buffer.from(expected);\n  const receivedBuf = Buffer.from(signature);\n  if (expectedBuf.length !== receivedBuf.length) return false;\n  return timingSafeEqual(expectedBuf, receivedBuf);\n}',
      rw: {
        ex: [
          'AWS KMS (key management + envelope encryption)',
          'HashiCorp Vault',
          'Stripe (AES-256-GCM for card data, PCI DSS Level 1)',
          'Auth0 (Argon2id for passwords)',
          '1Password (Argon2id + AES-256-GCM)',
          'Signal (double ratchet — end-to-end encryption)',
        ],
        cs: 'LinkedIn (2012 breach): 6.5M password hashes posted online. They had used unsalted SHA1 — 90% cracked within days using rainbow tables and GPU brute force. In 2016 the full breach was revealed: 117M accounts, most passwords cracked within weeks. Modern standard: Argon2id with per-user salt (random 16 bytes) means each password must be brute-forced individually with 64MB memory requirement per attempt. Even with a complete database dump, strong passwords are computationally impractical to crack.',
      },
    },
    interview: {
      q: 'Your team stores user passwords as SHA256(password). A colleague says SHA256 is cryptographically secure. How do you respond and what is the migration plan?',
      a: 'SHA256 IS cryptographically secure as a general-purpose hash function — for integrity checking, digital signatures, Merkle trees. But it is catastrophically wrong for passwords because it is fast: a modern GPU computes 10 billion SHA256 hashes per second. An attacker with your hashed password database can try all 8-character alphanumeric combinations in under 10 minutes. Password hashing requires an intentionally slow, memory-hard function. Use Argon2id with memoryCost=65536 (64MB) and timeCost=3 — this takes ~200ms per hash and requires 64MB RAM per parallel attempt, making GPU farming impractical. Also: unsalted SHA256 is doubly dangerous — identical passwords hash identically, enabling rainbow table attacks. Argon2id uses a unique per-user salt automatically. Migration plan without forcing all users to reset: 1. On next successful login with existing SHA256 hash: verify password against SHA256 hash (works). 2. Immediately re-hash with Argon2id and update the stored hash field. 3. Add a hash_version column (sha256 or argon2id) to track migration progress. 4. After 90 days, expire all remaining SHA256 hashes and force password reset for those accounts via email.',
      fu: [
        'What is the difference between symmetric and asymmetric encryption and when do you use each?',
        'Why must password hashes be salted?',
        'What is envelope encryption and how does AWS KMS implement it?',
        'What is AES-GCM and why is authenticated encryption (AEAD) important?',
      ],
    },
  },
  // ── Part 5 ──────────────────────────────────────────────────────────────────
  {
    id: 'auth-api-security',
    cat: 'security',
    color: '#ef4444',
    icon: '🛡️',
    title: 'API Security',
    tag: 'Securing APIs at every layer — BOLA, mass assignment, rate limiting, HMAC signing',
    overview: 'APIs are the primary attack surface of modern applications. API security requires defense in depth: TLS for transport, authentication for identity, rate limiting for availability, input validation to prevent injection, authorization on every resource access, and filtered responses to prevent data leakage. The OWASP API Security Top 10 documents the most common failures — BOLA is the #1 vulnerability, present in the vast majority of APIs.',
    components: [
      {
        name: 'BOLA Prevention',
        icon: '🚫',
        role: 'Broken Object Level Authorization — verify resource ownership on every endpoint',
        detail: 'if resource.userId !== req.user.id → 403 — the most common API vulnerability',
      },
      {
        name: 'Rate Limiting',
        icon: '⏱️',
        role: 'by IP + user ID + endpoint — 5 attempts/min on auth, 100/min on API',
        detail: 'use Redis sliding window counter — return 429 with Retry-After header',
      },
      {
        name: 'Input Validation',
        icon: '✅',
        role: 'Zod/Joi/Pydantic schema validation at every API boundary',
        detail: 'reject unexpected fields — prevent mass assignment and injection',
      },
      {
        name: 'Response DTO',
        icon: '📤',
        role: 'explicitly define returned fields — never serialize raw DB rows',
        detail: 'prevent accidental PII/internal data exposure',
      },
      {
        name: 'HMAC Request Signing',
        icon: '✍️',
        role: 'Stripe pattern: sign request body + timestamp with HMAC-SHA256',
        detail: 'receiver verifies — prevents replay attacks and ensures payload integrity',
      },
    ],
    howItWorks: 'Defense in depth — every request passes through security layers in order: 1. TLS (HTTPS): all traffic encrypted. 2. Rate limiting: Redis-based sliding window counter keyed by IP:endpoint — block early before processing. 3. Authentication: validate JWT signature, expiry, issuer. Or verify API key against hash in DB. 4. Input validation: Zod schema on request body — reject unknown fields (prevents mass assignment), validate types and lengths (prevents some injection). 5. Authorization — BOLA check: fetch resource, verify ownership. BFLA check: verify user role has permission for this operation. 6. Business logic: process the request. 7. Response filtering: serialize to DTO — only return fields the API contract specifies. Never return password_hash, internal IDs, other users data. HMAC webhook signing (Stripe pattern): sender computes: signature = HMAC-SHA256(timestamp + . + request_body, webhook_secret). Sends in header: Stripe-Signature: t=timestamp,v1=signature. Receiver recomputes HMAC and compares. Also validates timestamp is within 5 minutes (prevents replay). This ensures webhooks actually came from Stripe and were not tampered in transit. Mass assignment: POST /users with body {name:Alice, isAdmin:true} — if server does User.update(req.body), user becomes admin. Fix: use explicit allowlist of fields, never spread req.body onto model.',
    failures: [
      {
        name: 'BOLA (Broken Object Level Authorization)',
        cause: 'endpoint checks authentication but not resource ownership — symptom: GET /api/invoices/456 returns invoice belonging to user 456 when authenticated as user 123',
        symptom: 'GET /api/invoices/456 returns another user\'s invoice — attacker iterates IDs to harvest all user data',
        fix: 'every data endpoint must verify resource.userId === req.user.id — return 403 if not',
        severity: 'critical',
      },
      {
        name: 'Mass Assignment',
        cause: 'binding all request body fields to model without allowlist',
        symptom: 'attacker sends {name:Alice, isAdmin:true, credits:99999} in profile update — gets admin privileges',
        fix: 'whitelist accepted fields explicitly; never Object.assign(user, req.body)',
        severity: 'critical',
      },
      {
        name: 'Excessive Data Exposure',
        cause: 'returning entire DB row in API response',
        symptom: 'profile endpoint returns password_hash, internal_notes, other_user_ids — attacker harvests sensitive data',
        fix: 'create response DTOs — explicitly define what each endpoint returns',
        severity: 'high',
      },
      {
        name: 'No rate limiting on auth endpoints',
        cause: 'no throttling on /login, /forgot-password, /verify-otp',
        symptom: 'attacker attempts 10M passwords or OTP codes — brute forces credentials or MFA',
        fix: '5 attempts/min by IP, exponential backoff, CAPTCHA after 3 failures, alert on >50 failures/min',
        severity: 'high',
      },
    ],
    decision: {
      choose: [
        'Rate limiting at the API gateway layer (not just app code) — fail early before hitting compute',
        'HMAC signing for webhooks and B2B APIs where request integrity must be verifiable',
        'Schema validation on every endpoint input — make valid schemas explicit, reject everything else',
        'Structured security logging: every auth failure, every 403, every rate limit hit — log request ID, user ID, resource, reason',
      ],
      avoid: [
        'Trusting any client-supplied data for authorization decisions',
        'Returning raw database models in API responses',
        'Relying on obscurity (unguessable IDs) as an authorization substitute',
        'Generic error messages that leak implementation details (stack traces, SQL errors)',
      ],
      vs: [
        {
          name: 'API Keys',
          when: 'Server-to-server. Hash the key in DB (SHA256 lookup), never store plaintext. Rotate on exposure.',
        },
        {
          name: 'Bearer JWT',
          when: 'User-facing APIs. Short-lived access tokens validated by signature. Scale without DB call.',
        },
        {
          name: 'mTLS',
          when: 'High-security B2B APIs (financial, healthcare). Client certificate authentication. Most secure but highest setup complexity.',
        },
      ],
    },
    a: {
      v: 'Restaurant with Multiple Security Checkpoints',
      t: 'API security is like a restaurant with multiple checkpoints: doorman checks reservation (authentication), host checks you are at the right table not someone else seat (BOLA), waiter limits how many dishes you can order (rate limiting), kitchen validates the order makes sense (input validation), food runner only brings what you ordered not the whole menu (response filtering). Skip any checkpoint and the system breaks.',
      tx: 'No single security layer is sufficient — attackers probe each one independently. Authentication without authorization = BOLA. Authorization without input validation = injection. All of these without rate limiting = brute force. Defense in depth means every layer has one job.',
      s: 'TLS → Auth → Rate limit → Validate input → Authorize per resource → Filter response',
    },
    te: {
      def: 'API security is the set of controls protecting APIs from unauthorized access, data theft, abuse, and manipulation — including authentication, per-resource authorization (BOLA prevention), rate limiting, input validation, and response filtering.',
      types: [
        { n: 'BOLA/IDOR prevention', d: 'resource-level ownership check on every endpoint' },
        { n: 'Mass Assignment protection', d: 'explicit field allowlisting — never bind full request body to model' },
        { n: 'Rate Limiting', d: 'Redis sliding window by IP + user + endpoint' },
        { n: 'HMAC Request Signing', d: 'webhook and B2B API integrity — timestamp + body signed with shared secret' },
      ],
      when: 'Apply all API security controls from the first API endpoint. Retrofitting security is exponentially harder than building it in. Use API gateway (AWS API Gateway, Kong) for rate limiting and auth validation; use middleware for BOLA and input validation.',
      trade: 'Each security layer adds a small amount of processing time: Redis rate limit check (~1ms), schema validation (~1ms), auth token validation (~1ms). Total overhead: 3-5ms — completely acceptable. The alternative (no security) costs $10M+ in breach costs. Use API gateway to handle rate limiting and TLS at the network edge before requests reach application code.',
      code: 'import express from \'express\';\nimport helmet from \'helmet\';\nimport rateLimit from \'express-rate-limit\';\nimport { createHmac, timingSafeEqual } from \'crypto\';\nimport { z } from \'zod\';\n\nconst app = express();\napp.use(helmet()); // Security headers\napp.use(express.json());\n\n// Rate limiting on auth endpoint\nconst authLimiter = rateLimit({\n  windowMs: 60 * 1000, // 1 minute\n  max: 5, // 5 attempts/min\n  message: { error: \'Too many attempts\', retryAfter: 60 },\n  standardHeaders: true,\n});\n\n// Input validation schema\nconst UpdateProfileSchema = z.object({\n  name: z.string().min(1).max(100),\n  bio: z.string().max(500).optional(),\n  // isAdmin NOT here — prevents mass assignment\n});\n\n// BOLA check middleware\nasync function assertOwnership(req, res, next) {\n  const resource = await db.findById(req.params.id);\n  if (!resource) return res.status(404).json({ error: \'Not found\' });\n  if (resource.userId !== req.user.id) return res.status(403).json({ error: \'Forbidden\' });\n  req.resource = resource;\n  next();\n}\n\napp.post(\'/auth/login\', authLimiter, async (req, res) => {\n  // login logic\n});\n\napp.patch(\'/api/profile/:id\', authenticate, assertOwnership, async (req, res) => {\n  const parsed = UpdateProfileSchema.safeParse(req.body);\n  if (!parsed.success) return res.status(400).json({ error: parsed.error.flatten() });\n  const updated = await db.updateProfile(req.params.id, parsed.data); // only allowlisted fields\n  // Response DTO — never return raw DB row\n  return res.json({ id: updated.id, name: updated.name, bio: updated.bio });\n});\n\n// HMAC webhook verification (Stripe pattern)\nfunction verifyWebhook(payload: string, sigHeader: string, secret: string): boolean {\n  const [tPart, v1Part] = sigHeader.split(\',\');\n  const timestamp = tPart.replace(\'t=\', \'\');\n  const received = v1Part.replace(\'v1=\', \'\');\n  // Replay attack prevention: reject if >5 minutes old\n  if (Date.now() / 1000 - parseInt(timestamp) > 300) return false;\n  const expected = createHmac(\'sha256\', secret)\n    .update(timestamp + \'.\' + payload)\n    .digest(\'hex\');\n  return timingSafeEqual(Buffer.from(expected), Buffer.from(received));\n}',
      rw: {
        ex: [
          'Stripe (HMAC webhook signing)',
          'AWS API Gateway (rate limiting + auth)',
          'Kong (API gateway)',
          'Cloudflare WAF (DDoS + injection)',
          'GitHub (API keys + OAuth scopes)',
          'Twilio (HMAC request validation)',
        ],
        cs: 'Peloton API (2021): security researcher found that unauthenticated requests to /api/user/{userId} returned private user profiles — age, city, workout history, weight, and gender — for any user ID without any authentication. The API had authentication on some endpoints but not this one. It was not just an authorization failure — it was a complete authentication bypass. 3M+ users potentially affected. Root cause: likely a missing authentication middleware on that specific route. Fix: authentication and authorization middleware should be applied globally with explicit exclusions for public endpoints, not added per-endpoint where they can be forgotten.',
      },
    },
    interview: {
      q: 'Walk me through the API security layers you would implement for a fintech API handling payment operations.',
      a: 'For a fintech payment API, defense in depth is non-negotiable. Layer by layer: 1. Transport: TLS 1.3 everywhere. HSTS header. No plain HTTP even internally. 2. Rate limiting: API gateway enforces: 100 req/min for general endpoints, 5 req/min for payment initiation, 3 attempts/min for PIN verification. Redis-backed, by user ID not just IP (VPN users share IPs). 3. Authentication: For user-facing: short-lived JWT (15 min) via RS256. For B2B partners: mTLS client certificates — strongest. For webhooks we send: HMAC-SHA256 signed payloads with timestamp (Stripe pattern). 4. Input validation: Zod schemas on every endpoint. Payment amount: BIGINT cents only (no floats). Reject unknown fields (prevents mass assignment). 5. Authorization — two levels: BFLA: requireRole(payment_initiator) middleware. BOLA: verify payment.accountId belongs to authenticated user before processing. 6. Idempotency: every payment endpoint requires Idempotency-Key header — stored in Redis, returns cached response for duplicate requests (prevents double-charge). 7. Response filtering: response DTOs never include internal account IDs, processing fees, risk scores, or other accounts in the same organization. 8. Audit logging: every payment attempt logged with request ID, user, amount, result, and all authorization decisions. Ingested into SIEM with alerts on unusual patterns (large amount, new beneficiary, unusual hours).',
      fu: [
        'What is the difference between BOLA and BFLA in the OWASP API Security Top 10?',
        'How does HMAC request signing prevent replay attacks?',
        'What is the difference between CORS and CSRF and how do you mitigate each?',
        'How do you design rate limiting that is fair to legitimate users while blocking attacks?',
      ],
    },
  },
  {
    id: 'auth-secret-mgmt',
    cat: 'security',
    color: '#ef4444',
    icon: '🗝️',
    title: 'Secret Management',
    tag: 'Keeping API keys, DB passwords, and certificates out of code — and out of attackers hands',
    overview: 'Secret management is the discipline of storing, distributing, rotating, and auditing sensitive values — API keys, database credentials, TLS certificates, encryption keys — without ever committing them to source code or exposing them in logs. A single hardcoded credential can compromise an entire production system: GitHub secret scanning bots find exposed credentials within 4 minutes of a public commit on average.',
    components: [
      {
        name: 'Secret Store',
        icon: '🏦',
        role: 'HashiCorp Vault, AWS Secrets Manager, GCP Secret Manager',
        detail: 'encrypted at rest via KMS, access-controlled via IAM policies, every read audited in CloudTrail/Vault audit log',
      },
      {
        name: 'Dynamic Secrets',
        icon: '⏳',
        role: 'Vault generates temporary DB credentials per-service with 1-hour TTL',
        detail: 'automatically revoked after TTL — no long-lived DB passwords exist anywhere',
      },
      {
        name: 'Secret Rotation',
        icon: '🔄',
        role: 'AWS Secrets Manager rotates RDS passwords automatically',
        detail: 'old secret valid during rotation window — zero-downtime credential cycling',
      },
      {
        name: 'Envelope Encryption',
        icon: '📦',
        role: 'DEK per secret encrypted by KEK in KMS',
        detail: 'compromising one DEK exposes one secret, not the key hierarchy',
      },
      {
        name: 'Audit Trail',
        icon: '📋',
        role: 'every secret read/write logged with timestamp, caller identity, and source IP',
        detail: 'essential for SOC2, PCI DSS, HIPAA compliance and incident reconstruction',
      },
    ],
    howItWorks: 'Three patterns in order of maturity: Pattern 1 — Environment variables (minimum viable): secrets in OS env vars, injected at deploy time by CI/CD. Never in .env files committed to git. Limitation: no rotation, no audit, rotations require redeployment. Pattern 2 — Static secrets in manager: app calls AWS Secrets Manager at startup, fetches secret value, caches in memory. Rotation: AWS rotates automatically, app polls for new value or uses rotation hook (Lambda updates secret + restarts app). Limitation: secret still exists as a long-lived value, compromise window = rotation period. Pattern 3 — Dynamic secrets (HashiCorp Vault): 1. Service authenticates to Vault using its Kubernetes service account JWT (Vault K8s auth method). 2. Vault verifies the JWT against the K8s API server. 3. Vault generates a unique DB username+password valid for 1 hour (PostgreSQL dynamic secrets engine calls CREATE ROLE). 4. Service uses credentials. 5. After TTL: Vault calls DROP ROLE — credential no longer exists in the DB. Even if the service is compromised and credentials stolen, they expire in 1 hour. Envelope encryption for application-level encryption: 1. Generate random 32-byte DEK for each record group or S3 bucket. 2. Encrypt data with AES-256-GCM(DEK). 3. Encrypt DEK with KMS.Encrypt(KEK_id, DEK). 4. Store encrypted DEK alongside encrypted data. 5. To read: KMS.Decrypt(encrypted_DEK) → DEK → decrypt data. 6. Key rotation: KMS.ReEncrypt(encrypted_DEK, new_KEK_id) — no need to re-encrypt all data. Every KMS call logged in CloudTrail.',
    failures: [
      {
        name: 'Hardcoded secrets in source code',
        cause: 'developer committed DB password or API key',
        symptom: 'GitHub bots find it within 4 minutes; attacker has prod access',
        fix: 'pre-commit hooks (detect-secrets, gitleaks) block commits with high-entropy strings; rotate immediately on exposure; move to Secrets Manager',
        severity: 'critical',
      },
      {
        name: 'Secrets in application logs',
        cause: 'logging request/response objects that contain auth headers or body with credentials',
        symptom: 'log aggregation system (Datadog, CloudWatch) stores credentials in plaintext; often shared broadly',
        fix: 'redact sensitive fields before logging; never log Authorization headers; use structured logging with explicit field inclusion',
        severity: 'critical',
      },
      {
        name: 'Long-lived static credentials with no rotation',
        cause: 'DB password set once, never rotated because rotation requires downtime',
        symptom: 'breach from years ago is discovered only now because credentials were never cycled',
        fix: 'automate rotation with AWS Secrets Manager + RDS integration; implement dual-credential rotation (new password created, both valid briefly, old retired)',
        severity: 'high',
      },
    ],
    decision: {
      choose: [
        'AWS Secrets Manager for AWS-native workloads with automatic RDS rotation',
        'HashiCorp Vault for multi-cloud or on-premise with dynamic secrets (gold standard)',
        'Pre-commit hooks on every developer machine to prevent accidental secret commits',
        'IRSA/Workload Identity for K8s — pods authenticate to Secrets Manager using their service account, no static credentials needed',
      ],
      avoid: [
        '.env files in git repositories — even private repos have breach risk and are often shared',
        'Logging secrets even at debug level — logs are broadly accessible and often not encrypted at rest',
        'Long TTL on dynamic secrets — 1 hour max for DB credentials, 15 minutes for particularly sensitive ops',
      ],
      vs: [
        {
          name: 'AWS Secrets Manager',
          when: 'AWS workloads. Built-in RDS/Redshift rotation. $0.40/secret/month. Simple SDK.',
        },
        {
          name: 'HashiCorp Vault',
          when: 'Multi-cloud, on-premise, or dynamic secrets needed. More complex ops but most powerful.',
        },
        {
          name: 'Doppler',
          when: 'Developer-friendly secrets sync to AWS/GCP/Heroku. Good for small teams transitioning from .env.',
        },
      ],
    },
    a: {
      v: 'Bank Vault with Visitor Log',
      t: 'Secrets management is like a bank vault with a strict visitor log. You never photocopy your valuables and leave copies around the office — you keep them locked, grant time-limited access only when needed, log every visitor, and rotate the combination regularly. Dynamic secrets are even better: the vault generates a unique key for each visitor that only works for 1 hour then self-destructs.',
      tx: 'The goal is zero long-lived credentials in code, config files, or environment variables. Every secret has an owner, a TTL, and a complete audit trail. If any secret leaks, the blast radius is bounded by its TTL and its scope.',
      s: 'App → Vault auth → Dynamic cred (TTL 1hr) → DB → Vault auto-revokes',
    },
    te: {
      def: 'Secret management is the systematic process of creating, distributing, storing, rotating, and auditing sensitive credentials and cryptographic keys — ensuring secrets are never exposed in source code, logs, or unauthorized systems, and every access is traceable.',
      types: [
        { n: 'Static Secrets in Manager', d: 'long-lived credentials in Secrets Manager, better than code but require rotation policy' },
        { n: 'Dynamic Secrets', d: 'Vault generates per-request short-lived credentials — minimizes breach impact' },
        { n: 'Envelope Encryption', d: 'DEK+KEK hierarchy — application-level encryption without key sprawl' },
        { n: 'Workload Identity', d: 'K8s IRSA/Workload Identity — pods authenticate via service account, no static credential needed' },
      ],
      when: 'Use a secret manager from day one — retrofitting is painful and risky. Rotate all secrets immediately on any exposure. Use dynamic secrets for any credential that supports it (databases, cloud providers, Vault-integrated services). Use workload identity wherever possible to eliminate static credentials entirely.',
      trade: 'Fetching secrets at startup adds 100-300ms (acceptable — cache in memory). KMS decrypt per data read: ~5ms (cache decrypted DEKs with short TTL). Dynamic secrets add operational dependency on Vault availability — run Vault in HA mode (3-node cluster, Raft storage). The complexity is the price of not having a breach cost you $10M+ and your company reputation.',
      code: 'import { SecretsManagerClient, GetSecretValueCommand } from \'@aws-sdk/client-secrets-manager\';\n\nconst client = new SecretsManagerClient({ region: \'us-east-1\' });\n\n// Fetch secret from AWS Secrets Manager at startup — cache in memory\nasync function getSecret(secretName: string): Promise<string> {\n  try {\n    const response = await client.send(\n      new GetSecretValueCommand({ SecretId: secretName })\n    );\n    if (response.SecretString) return response.SecretString;\n    throw new Error(\'Secret has no string value\');\n  } catch (err) {\n    console.error(\'Failed to fetch secret:\', secretName, err);\n    throw err;\n  }\n}\n\n// ANTI-PATTERN: secret in code or .env committed to git\n// const DB_PASSWORD = \'mypassword123\'; // NEVER\n// const DB_PASSWORD = process.env.DB_PASSWORD; // Better, but still static\n\n// PATTERN: fetch from Secrets Manager at startup\nlet dbPassword: string;\nasync function initApp() {\n  dbPassword = await getSecret(\'prod/myapp/db-password\');\n  // Now connect to DB with the fetched credential\n  // Rotate: AWS Secrets Manager auto-rotates — app polls or uses Lambda hook\n}\n\n// Dynamic secrets concept (HashiCorp Vault):\n// 1. App authenticates to Vault with K8s service account JWT\n// 2. Vault issues: { username: \'v-app-abc123\', password: \'xyz789\', ttl: \'1h\' }\n// 3. App uses credentials for 1 hour\n// 4. Vault auto-calls DROP ROLE v-app-abc123 after TTL\n// 5. Even if stolen, credentials self-destruct in ≤1 hour',
      rw: {
        ex: [
          'HashiCorp Vault (dynamic secrets, PKI, encryption as a service)',
          'AWS Secrets Manager (managed rotation + RDS integration)',
          'GCP Secret Manager (IAM-controlled, version management)',
          'Doppler (developer-friendly, CI/CD integration)',
          '1Password Secrets Automation (team-friendly)',
          'cert-manager (K8s TLS certificate lifecycle)',
        ],
        cs: 'Toyota Connected (2022): a subsidiary accidentally published source code containing hardcoded credentials to a public GitHub repository. The credentials gave access to customer data for 296,019 users. The repository had been public for 5 years before discovery. Prevention: 1. Pre-commit hook (gitleaks/detect-secrets) would have blocked the initial commit. 2. GitHub Advanced Security secret scanning would have alerted within minutes. 3. AWS CloudTrail would have showed unusual access patterns years earlier. 4. Dynamic secrets would have meant no long-lived credential existed to steal — only a 1-hour window of exposure even if the credential was found. The 5-year exposure was only possible because credentials never rotated and no audit monitoring was in place.',
      },
    },
    interview: {
      q: 'A junior engineer committed a database password to a public GitHub repo. It has been there for 3 hours. Walk through your complete incident response.',
      a: 'Treat this as a confirmed breach — assume the credential was read. Step 1 — Rotate immediately (do not investigate first — rotate first): Change the database password right now. This closes the active attack vector. Step 2 — Revoke and invalidate: Terminate all existing DB connections using the old password. Check for any other systems using this credential. Step 3 — Remove from git history: git filter-branch or BFG Repo Cleaner to purge the commit from history. Force-push. Note: GitHub caches content — contact GitHub support to clear their cache. The public internet may have already indexed it (Common Crawl, Wayback Machine). Step 4 — Audit access logs: Pull DB query logs for the 3-hour window. Look for: unusual source IPs, unusual query patterns (bulk SELECT *, DROP TABLE attempts, large data exports). Pull CloudTrail for S3/IAM activity if the DB had cross-service permissions. Step 5 — Assess blast radius: What data was in this database? Was it PII? Payment data? If yes: GDPR/PCI DSS breach notification requirements (notify within 72 hours for GDPR). Step 6 — Prevent recurrence: Add detect-secrets pre-commit hook to all developer machines. Enable GitHub Advanced Security secret scanning on all repositories (automatic alerts). Add to developer onboarding and security training. Move all production credentials to AWS Secrets Manager — no credentials in code or env files. Step 7 — Post-mortem (blameless): Why was there no secret management process? What tooling was missing? Remediate the process, not just this one instance.',
      fu: [
        'What is envelope encryption and how does AWS KMS implement it without ever exposing the master key?',
        'How do dynamic secrets in HashiCorp Vault reduce blast radius compared to static credentials?',
        'What is IRSA (IAM Roles for Service Accounts) and how does it eliminate static credentials for K8s pods?',
        'How do you prevent secrets from appearing in application logs?',
      ],
    },
  },
  {
    id: 'auth-zero-trust',
    cat: 'security',
    color: '#ef4444',
    icon: '🚫',
    title: 'Zero Trust Architecture',
    tag: 'Never trust, always verify — the security model that eliminated the perimeter',
    overview: 'Zero Trust replaces the castle-and-moat security model (trust everything inside the corporate network) with never trust, always verify. Every request — from inside or outside — is authenticated, authorized, encrypted, and logged. Adopted by Google (BeyondCorp, after the 2010 Operation Aurora attack), required by US federal agencies (NIST 800-207, Biden executive order 2021), and now the baseline expectation for any organization handling sensitive data.',
    components: [
      {
        name: 'Identity Provider',
        icon: '🪪',
        role: 'Okta, Azure AD, Google Workspace — every user and service has a verified, auditable digital identity',
        detail: 'The foundation of Zero Trust. Every human and service principal has a unique identity with full audit trail. SSO enforces central policy. SCIM automates provisioning/deprovisioning so access is removed the moment an employee leaves.',
      },
      {
        name: 'Policy Engine',
        icon: '⚖️',
        role: 'Evaluates: who is the user, what device, what location, what time, what resource, what action — returns allow/deny with reason',
        detail: 'The decision point. Ingests signals from IdP, MDM, threat intelligence, and SIEM. Applies least-privilege policies. Every access decision is logged with full context. OPA (Open Policy Agent) is a popular open-source policy engine for service-to-service authorization.',
      },
      {
        name: 'Device Trust via MDM',
        icon: '💻',
        role: 'Jamf for macOS, Intune for Windows — verifies: is OS patched, is disk encrypted, is EDR installed, is corp-managed',
        detail: 'Device posture is a trust signal. A corp-managed, fully patched device with EDR running gets higher trust than an unmanaged personal device. Non-compliant devices can be blocked or given limited access (e.g., read-only, no sensitive data).',
      },
      {
        name: 'Micro-segmentation',
        icon: '🧱',
        role: 'Each service only accepts traffic from explicitly allowed sources — even if Service A is compromised, it cannot reach Service B',
        detail: 'Default-deny network policies in Kubernetes. Service mesh authorization policies (Istio AuthorizationPolicy). Even if an attacker compromises one pod, lateral movement is blocked by policy — they cannot reach the database or adjacent services.',
      },
      {
        name: 'Continuous Verification',
        icon: '🔄',
        role: 'Trust is not granted at login and held — re-evaluated on each request based on current risk signals: new device, anomalous location, high-value operation triggers step-up auth',
        detail: 'Session tokens are short-lived. Risk signals are evaluated continuously. High-value operations (wire transfer, admin action, bulk data export) trigger step-up authentication (re-verify FIDO2). Anomalous signals (new country, 3am access) trigger alert + additional verification.',
      },
    ],
    howItWorks: 'Traditional model: VPN gives full internal network access. If VPN credentials are phished, attacker reaches everything.\n\nZero Trust model:\n1. Device posture check: is this device corp-managed, OS patched, disk encrypted, no known malware?\n2. Identity verification: user authenticates with SSO + MFA (FIDO2 for phishing resistance).\n3. Context evaluation: location, time, risk score.\n4. Policy engine evaluates all signals — grants access ONLY to the specific resource requested.\n5. All traffic encrypted: TLS for user-to-app, mTLS between services via Istio SPIFFE certs.\n6. All access logged: SIEM ingests every access decision for anomaly detection.',
    failures: [
      {
        name: 'VPN as the only perimeter',
        cause: 'Assuming corporate network = trusted',
        symptom: 'One phished VPN credential gives attacker lateral movement to all internal systems',
        fix: 'Zero Trust replaces VPN — identity + device posture replaces network location as trust signal',
        severity: 'critical',
      },
      {
        name: 'Missing micro-segmentation',
        cause: 'All internal services can reach all other services',
        symptom: 'Single compromised service gives attacker access to database, internal APIs, secrets',
        fix: 'Default-deny network policies in K8s, service mesh authorization policies',
        severity: 'critical',
      },
      {
        name: 'Zero Trust theater',
        cause: 'Deploying a Zero Trust product without redesigning access model',
        symptom: 'All services in the Zero Trust perimeter but still overprivileged',
        fix: 'Zero Trust requires least-privilege access redesign, not just a new product',
        severity: 'medium',
      },
    ],
    decision: {
      choose: [
        'Organizations moving away from VPN-centric architecture',
        'Remote/hybrid workforces accessing internal tools from any network',
        'Microservices architectures requiring service-to-service authentication',
        'Any organization handling sensitive regulated data (healthcare, finance, government)',
      ],
      avoid: [
        'Buying a Zero Trust product without redesigning access model — Zero Trust is a strategy, not a product',
        'Applying Zero Trust only to external access while keeping flat internal networks',
        'Implementing without centralized logging — Zero Trust requires observability to be effective',
      ],
      vs: [
        { name: 'BeyondCorp (user-focused)', when: 'Replace VPN for human users. Identity + device posture grants per-application access. Google model.' },
        { name: 'ZTNA', when: 'Cloudflare Access, Zscaler, Tailscale. Software-defined perimeter for remote users.' },
        { name: 'Service Mesh ZT', when: 'Istio/Linkerd for service-to-service. mTLS + SPIFFE identity + OPA policy for east-west traffic.' },
      ],
    },
    a: {
      v: 'Airport Security vs Hotel Lobby',
      t: 'Traditional security is a hotel lobby: show ID once at the door, then walk anywhere freely. Zero Trust is airport security: show boarding pass at every gate, ID at security, passport at international departures — each checkpoint independently verifies you belong there.',
      tx: 'Once inside the perimeter, attackers move freely. Zero Trust makes every resource its own perimeter. Breach of one system cannot be leveraged to access others.',
      s: 'Request → Identity check → Device check → Context → Policy Engine → Only this resource',
    },
    te: {
      def: 'Zero Trust is a security model (NIST 800-207) based on the principle that no user, device, or service is trusted by default regardless of network location. Every access request is authenticated, authorized based on least privilege, encrypted, and logged.',
      types: [
        { n: 'BeyondCorp', d: 'User-focused ZT: identity + device replaces VPN — Google model' },
        { n: 'ZTNA', d: 'Zero Trust Network Access: software-defined perimeter — Cloudflare Access, Zscaler, Tailscale' },
        { n: 'Service Mesh', d: 'East-west ZT: mTLS + SPIFFE + OPA for service-to-service' },
        { n: 'Continuous Adaptive Trust', d: 'Risk-based: trust score recalculated continuously, high-value operations trigger step-up auth' },
      ],
      when: 'Begin Zero Trust migration when: 1. Removing VPN dependency, 2. Enabling remote work securely, 3. Building microservices, 4. Handling regulated data. Adopt incrementally: identity foundation first, then device trust, then ZTNA, then micro-segmentation.',
      trade: 'Significant implementation complexity and organizational change management. Requires investment in IdP, MDM, policy engine, observability stack. A 12-18 month migration for a mid-size company. Start with the highest-risk systems and expand. Cost is justified: Zero Trust is the only model that limits blast radius when breach occurs.',
      code: '# Istio AuthorizationPolicy — orders-service can call payments-service\napiVersion: security.istio.io/v1beta1\nkind: AuthorizationPolicy\nmetadata:\n  name: payments-service-policy\nspec:\n  selector:\n    matchLabels:\n      app: payments-service\n  action: ALLOW\n  rules:\n    - from:\n        - source:\n            principals:\n              - cluster.local/ns/default/sa/orders-service\n      to:\n        - operation:\n            methods: [\'POST\']\n            paths: [\'/v1/payments\']\n---\n# Policy engine evaluation (pseudo-code)\nconst decision = await policyEngine.evaluate({\n  subject: { userId: req.user.id, deviceId: req.device.id },\n  resource: { service: \'payments\', endpoint: \'/v1/payments\' },\n  context: {\n    deviceCompliant: req.device.isCompliant,\n    mfaVerified: req.auth.mfaVerified,\n    riskScore: await riskEngine.score(req),\n  },\n});\nif (!decision.allow) return res.status(403).json({ error: \'Access denied\' });',
      rw: {
        ex: [
          'Google BeyondCorp (2014, eliminated VPN for all engineers)',
          'Cloudflare Access (ZTNA)',
          'Zscaler ZPA (zero trust private access)',
          'Tailscale (WireGuard-based mesh with identity)',
          'Istio (service mesh mTLS)',
        ],
        cs: 'Google deployed BeyondCorp after Operation Aurora (2010) — a Chinese state-sponsored attack that breached Google and 20+ companies by exploiting a single IE zero-day on an employee machine, then moving laterally through the trusted corporate network. BeyondCorp took 6 years to fully deploy. Every Google engineer now accesses internal tools from any network based solely on identity (Google SSO + hardware security key) and device certificate. No VPN.',
      },
    },
    interview: {
      q: 'Your company uses VPN for internal tool access. Post-breach analysis shows an attacker used one phished VPN credential to access your database, admin panel, and internal APIs over 3 weeks. Design a Zero Trust migration plan.',
      a: 'Phase 1 (0-3 months): Identity foundation. Deploy SSO (Okta/Azure AD), enforce MFA everywhere (FIDO2 for admin accounts), eliminate shared credentials. This alone closes the phishing vector that caused this breach.\n\nPhase 2 (3-6 months): Device trust. MDM enrollment (Jamf/Intune). Define compliance policies: OS must be patched, disk encrypted, EDR installed.\n\nPhase 3 (6-9 months): Replace VPN for highest-risk applications first. Deploy Cloudflare Access for admin panel and production DB access. Users authenticate with SSO + FIDO2 — no VPN.\n\nPhase 4 (9-12 months): Micro-segment internal services. Deploy Istio with mTLS between all services. OPA policies: payments service can only be called by checkout service — nothing else. Default deny.\n\nPhase 5 (12-18 months): Continuous monitoring. SIEM ingesting all access logs. Anomaly detection alerts. Decommission VPN.',
      fu: [
        'What is SPIFFE/SVID and how does it provide service identity in a Zero Trust service mesh?',
        'What is the difference between ZTNA and a traditional VPN?',
        'How does continuous adaptive trust (risk-based authentication) work?',
        'How do you implement Zero Trust for contractors who use their own devices?',
      ],
    },
  },
  {
    id: 'auth-mfa',
    cat: 'security',
    color: '#ef4444',
    icon: '📱',
    title: 'Multi-Factor Authentication (MFA)',
    tag: 'Something you know + something you have + something you are — blocking 99.9% of account takeovers',
    overview: 'Multi-Factor Authentication requires two or more verification factors from different categories: knowledge (password), possession (phone, hardware key), or inherence (biometric). Microsoft reports MFA blocks 99.9% of automated account takeover attacks. Understanding the security properties — and failure modes — of each MFA method is essential for architects: TOTP is phishable, FIDO2 is not, and SMS is the weakest option.',
    components: [
      {
        name: 'TOTP',
        icon: '🔢',
        role: 'Time-based OTP, RFC 6238: HMAC-SHA1(secret, floor(unix_time/30)) mod 10^6 — 6-digit code rotating every 30 seconds',
        detail: 'Widely supported. Works offline. Phishable via AiTM proxy — attacker relays the code to the real site within the 30-second window. If server DB is breached, all TOTP secrets are compromised. Mitigate by encrypting secrets at rest.',
      },
      {
        name: 'FIDO2/WebAuthn',
        icon: '🔑',
        role: 'W3C standard: asymmetric key pair per domain — private key never leaves the device, domain-bound so fake sites get nothing — phishing-proof by cryptographic design',
        detail: 'The only phishing-proof MFA. Credential is bound to the exact relying party ID (domain). A credential registered for example.com cannot be used on evilexample.com — the browser enforces this. Hardware keys (YubiKey) or platform authenticators (Touch ID, Windows Hello).',
      },
      {
        name: 'SMS OTP',
        icon: '📨',
        role: 'One-time code via text message — weakest MFA — vulnerable to SIM swap attacks where attacker ports your number',
        detail: 'Better than no MFA but the weakest form. SIM swap: attacker calls carrier, social-engineers them into porting the victim number. NIST 800-63 deprecated SMS OTP for high-assurance applications.',
      },
      {
        name: 'Push Notification',
        icon: '📲',
        role: 'Duo, Okta Verify — user taps Approve on phone — vulnerable to MFA fatigue attacks: attacker spams approval requests until user accidentally approves',
        detail: 'Convenient UX. Vulnerable to MFA fatigue (push bombing). Fix: number matching (user must type a number shown in the browser into the app — attacker cannot automate this), context, rate limiting.',
      },
      {
        name: 'Recovery Codes',
        icon: '🗝️',
        role: 'One-time backup codes generated at MFA setup — stored as bcrypt hashes in DB — essential UX for lost devices',
        detail: 'Critical for UX: without recovery codes, locked-out users create support burden. Each code is single-use, generated with crypto.randomBytes(32), stored as bcrypt hash. Show once at generation, never again.',
      },
    ],
    howItWorks: 'TOTP flow:\n1. At setup: server generates secret (random 20 bytes), user scans QR code in Google Authenticator/Authy.\n2. Both server and authenticator independently compute: HOTP(secret, floor(current_unix_time / 30)).\n3. On login: user enters 6-digit code, server verifies it matches (with +/-1 time step tolerance for clock skew).\n\nFIDO2/WebAuthn flow:\n1. Registration: browser generates key pair on authenticator. Public key sent to server. Private key never leaves device.\n2. Authentication: server sends a challenge. Authenticator signs it with private key (requires user gesture: touch, PIN, biometric). Server verifies signature.\n\nDomain binding: credential created for example.com cannot be used on evilexample.com — the browser checks the domain. This makes phishing cryptographically impossible.',
    failures: [
      {
        name: 'TOTP phished via AiTM',
        cause: 'TOTP code valid for 30 seconds — attacker relays to real site instantly via reverse proxy',
        symptom: 'Attacker creates valid session despite MFA',
        fix: 'Switch to FIDO2/WebAuthn for admin and high-value accounts — phishing-resistant by design',
        severity: 'critical',
      },
      {
        name: 'SMS SIM swap',
        cause: 'Attacker socially engineers carrier to port victim number to attacker SIM',
        symptom: 'Attacker receives all SMS OTPs for account',
        fix: 'Disable SMS MFA for sensitive accounts; use TOTP minimum, FIDO2 for high-value',
        severity: 'high',
      },
      {
        name: 'MFA fatigue',
        cause: 'Push notification MFA with no context or rate limiting',
        symptom: 'Attacker sends 20+ push requests, exhausted user approves one',
        fix: 'Number matching, add-context approvals, rate limit push requests, alert on multiple denials',
        severity: 'high',
      },
    ],
    decision: {
      choose: [
        'FIDO2/WebAuthn for all admin accounts and high-value operations — only phishing-proof MFA',
        'TOTP as minimum MFA for all user accounts',
        'Risk-based MFA (step-up authentication): trigger re-verification only when risk is elevated',
        'Number matching for push notification MFA to prevent fatigue attacks',
      ],
      avoid: [
        'SMS OTP for any sensitive system — SIM swap bypasses it',
        'Allowing users to fall back to SMS when FIDO2 is unavailable for admin accounts',
        'No rate limiting on MFA attempts (brute force TOTP time windows)',
      ],
      vs: [
        { name: 'FIDO2 Hardware Key (YubiKey)', when: 'Highest security. Phishing-proof. No shared secret. Physical possession required. For admin, finance, security team.' },
        { name: 'Platform Authenticator (Touch ID/FaceID)', when: 'FIDO2-based. Synced via iCloud/Google for passkeys. Convenient for all users. Phishing-proof.' },
        { name: 'TOTP App', when: 'Minimum acceptable for general users. Not phishing-proof but blocks automated attacks. Wide compatibility.' },
      ],
    },
    a: {
      v: 'Safe Deposit Box',
      t: 'A password alone is a padlock — anyone with a copy of the key can open it. MFA is a bank safe deposit box: you need both your key AND the bank manager key. FIDO2 is even better: the bank key is cryptographically tied to this specific bank branch — a fake branch cannot use it.',
      tx: 'Even the strongest password can be phished or leaked. MFA ensures knowing the password is never sufficient. FIDO2 goes further: even if the user is fooled by a fake site, the credential cannot be used on the wrong domain.',
      s: 'Password (know) + TOTP/FIDO2 (have) + biometric (are) = access granted',
    },
    te: {
      def: 'MFA requires users to prove identity using two or more factors from distinct categories: knowledge (password/PIN), possession (phone/hardware key), or inherence (biometric). The combination makes account takeover exponentially harder even with a stolen password.',
      types: [
        { n: 'TOTP/RFC 6238', d: 'Time-based OTP — shared secret, 30-second window, phishable in real time by AiTM' },
        { n: 'FIDO2/WebAuthn', d: 'Asymmetric, domain-bound, phishing-proof — hardware key or platform authenticator' },
        { n: 'Passkeys', d: 'FIDO2 synced via iCloud/Google Password Manager — passwordless + phishing-proof' },
        { n: 'Push MFA', d: 'Duo/Okta Verify — convenient but vulnerable to fatigue attacks without number matching' },
      ],
      when: 'Enforce MFA for ALL accounts. Use FIDO2 for admin and privileged accounts — mandatory. For regular users: TOTP minimum. Offer passkeys as a better UX. Disable SMS MFA for any account with production access or payment authority.',
      trade: 'MFA adds friction to login — balance with UX: device trust (remember this device 30 days), risk-based step-up (only challenge on new device/location/high-value action), passkeys (passwordless + phishing-proof + no TOTP code to type). Security is a product feature: 99.9% attack block rate is a compelling value proposition.',
      code: '// TOTP verification with otplib\nimport { authenticator } from \'otplib\';\n\nconst secret = authenticator.generateSecret(); // Store encrypted in DB\nconst otpauthUrl = authenticator.keyuri(userEmail, \'MyApp\', secret);\n// Show QR code of otpauthUrl to user during setup\n\n// Verify on login\nconst isValid = authenticator.check(userProvidedCode, secret);\nif (!isValid) throw new Error(\'Invalid TOTP code\');\n\n// WebAuthn verification with simplewebauthn\nimport { verifyAuthenticationResponse } from \'@simplewebauthn/server\';\n\nconst verification = await verifyAuthenticationResponse({\n  response: req.body,\n  expectedChallenge: session.challenge,\n  expectedOrigin: \'https://example.com\', // must match exactly\n  expectedRPID: \'example.com\',\n  authenticator: {\n    credentialID: storedCredential.id,\n    credentialPublicKey: storedCredential.publicKey,\n    counter: storedCredential.signCount,\n  },\n});\nif (!verification.verified) throw new Error(\'WebAuthn verification failed\');',
      rw: {
        ex: [
          'Google (FIDO2 + passkeys — eliminated account takeover for employees)',
          'GitHub (TOTP + hardware security keys — required for all npm package maintainers)',
          'Stripe (enforces MFA for all dashboard access)',
          'Twilio (post-breach adopted FIDO2 for all employees)',
          'Apple (passkeys synced via iCloud Keychain)',
        ],
        cs: 'Twilio breach (August 2022): attackers sent SMS messages to Twilio employees impersonating IT. The phishing page was a real-time AiTM proxy that relayed credentials and TOTP codes to real Okta instantly. 163 customer accounts were compromised. If Twilio had required FIDO2 hardware keys for all employees (they now do, post-breach), the attack would have failed: FIDO2 credentials are domain-bound — the attacker domain would not match Okta domain, and authentication would fail even with correct credentials and real-time TOTP relay.',
      },
    },
    interview: {
      q: 'Your admin dashboard uses TOTP-based MFA. A security researcher demonstrates they can bypass it using a real-time phishing proxy. How do you fix it and prevent future bypasses?',
      a: 'The attack is AiTM phishing: attacker runs a reverse proxy that looks identical to your login page. User enters credentials + TOTP code → proxy relays them instantly → attacker gets a valid session. TOTP is valid for 30 seconds — plenty of time.\n\nFix: migrate admin accounts from TOTP to FIDO2/WebAuthn. FIDO2 is phishing-proof by design: the credential is cryptographically bound to the exact relying party ID (your actual domain). When the user tries to authenticate on the attacker domain, the browser has no matching credential — authentication fails.\n\nMigration plan:\n1. Issue hardware security keys (YubiKey 5) to all admin/engineering accounts.\n2. Register FIDO2 credentials in your IdP (Okta, Auth0).\n3. Enforce FIDO2 as the ONLY MFA option for admin accounts — remove TOTP fallback.\n4. For regular users: implement number matching on push notifications.\n5. Add login anomaly detection: new device + failed MFA + unusual location triggers alert + account lock.',
      fu: [
        'What is the TOTP algorithm (RFC 6238) — how is the 6-digit code computed?',
        'How do passkeys work and how are they different from hardware security keys?',
        'What is a SIM swap attack and which account types are most vulnerable?',
        'How do you implement risk-based (adaptive) authentication?',
      ],
    },
  },
];
