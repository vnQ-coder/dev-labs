import { Concept } from '../types';

export const CONCEPTS_NETWORKING: Concept[] = [
  {
    id: 'url-journey',
    cat: 'networking',
    color: '#6272d4',
    icon: '-',
    title: "You hit Enter. What happens in the next 200 milliseconds?",
    tag: "The full hidden journey from keypress to pixel",
    hook: "The moment you press Enter in your browser's address bar, a 10-step operation fires off before a single pixel appears on your screen. Your browser contacts a name server thousands of miles away to convert a domain into an IP address, opens a socket to a server on another continent, negotiates an encryption scheme that has never been used before, sends a request, waits for code to run and databases to respond, receives a stream of HTML, parses it, fetches dozens of sub-resources, and finally renders a page — all in under 200 milliseconds. Nobody tells you this is happening. That invisibility is engineered. This is what's underneath.",
    simpleExplain: "Think of loading a website like ordering pizza. First, you look up the restaurant's phone number — that's DNS, translating 'google.com' into an actual address the internet can use. Then you call and the phone rings three times before anyone picks up — that's the TCP handshake, making sure a real connection exists. Next, the restaurant checks your ID to make sure you're calling a real, legitimate place — that's TLS verifying the certificate. Then you place your order — that's the HTTP request. You wait while they cook — that's the server running code and querying databases. The delivery driver arrives with the pizza — that's the HTTP response sending back HTML. And finally you open the box and eat — that's the browser rendering the page you see.",
    howItWorks: "Each layer of the request lifecycle is handled by a separate protocol that is completely unaware of the others. DNS operates over UDP and returns an IP address in tens of milliseconds when cached, or 100–300ms when walking the resolver chain. TCP's three-way handshake adds one round-trip time to establish sequence numbers before any data flows. TLS 1.3 adds another RTT for the cryptographic handshake — ClientHello carries the client's ECDHE key share so the server can respond with its own key share and the server's Finished message in a single round trip. HTTP/2 multiplexes all requests over a single TLS connection, eliminating the per-request overhead of HTTP/1.1. The browser's rendering pipeline then runs in parallel with the network — parsing HTML into the DOM, building the CSSOM from CSS, merging into a render tree, laying out elements, painting pixels, and compositing layers — and every render-blocking resource delays the entire pipeline.",
    pipeline: [
      {
        icon: 'Monitor',
        label: 'Browser Cache',
        simple: "Before doing any work, your browser checks whether it visited this page recently and still has a fresh copy saved.",
        deep: "Cache-Control headers from the previous response determine whether a stored copy is still valid. A cache hit (200 from cache) means zero network calls — the browser serves the stored response immediately. max-age and s-maxage control freshness; must-revalidate forces a conditional GET with If-None-Match on stale entries.",
      },
      {
        icon: 'Search',
        label: 'DNS Lookup',
        simple: "Your browser needs to translate 'google.com' into a numeric address like 142.250.80.46 that routers understand.",
        deep: "The recursive resolver walks a chain from root servers to TLD servers to authoritative nameservers if no cached answer exists. Each hop is cached by its TTL — typically 60–300 seconds for A records. The final A or AAAA record is what the browser uses to open a socket.",
      },
      {
        icon: 'Plug',
        label: 'TCP Connect',
        simple: "Your browser knocks on the server's door three times before either side says a word — that's just how TCP works.",
        deep: "The three-way handshake (SYN to SYN-ACK to ACK) establishes sequence numbers so both sides can detect out-of-order or lost packets. This costs one full round-trip time (RTT) before any application data flows. Over a 50ms RTT link, TCP setup alone costs 50ms.",
      },
      {
        icon: 'Lock',
        label: 'TLS Handshake',
        simple: "Both sides agree on a secret encryption code that nobody watching the wire could ever crack, and they do it without whispering it to each other.",
        deep: "TLS 1.3 ClientHello carries the client's ECDHE key share so the server can complete the key exchange in its first response. The server replies with ServerHello, its certificate, CertificateVerify, and a Finished message — all in one RTT. Derived symmetric keys are used for all subsequent data. Session resumption (0-RTT) can skip the handshake entirely for returning clients.",
      },
      {
        icon: 'ArrowRight',
        label: 'HTTP Request',
        simple: "Your browser says 'please give me this page' in a very specific format the server understands.",
        deep: "An HTTP/2 GET request carries headers (Host, Accept-Encoding, Cookie, User-Agent) compressed via HPACK. HTTP/2 multiplexes multiple requests over a single TLS connection using independent streams, eliminating head-of-line blocking at the HTTP layer. HTTP/3 (QUIC) moves this multiplexing to the transport layer, eliminating TCP-level head-of-line blocking as well.",
      },
      {
        icon: 'Server',
        label: 'Server Processing',
        simple: "The server runs your application's code, queries the database, and assembles an answer to send back.",
        deep: "A typical stack: Nginx reverse proxy forwards the request to an application process (Node.js, Python, Go). The app executes business logic, issues SQL or NoSQL queries, formats results, and serializes a response. End-to-end server processing typically takes 5–50ms for cached data and 50–500ms for cold database queries. This is the layer most affected by slow SQL and N+1 query problems.",
      },
      {
        icon: 'ArrowLeft',
        label: 'HTTP Response',
        simple: "The server sends everything back — HTML, styles, scripts — and tells your browser how long to remember it.",
        deep: "HTTP 200 OK carries Content-Type, Cache-Control, ETag, and Set-Cookie headers alongside the body. Chunked transfer encoding lets the server begin sending HTML before it has finished generating the full response. HTTP/2 server push can proactively send CSS and JS resources before the browser requests them.",
      },
      {
        icon: 'Monitor',
        label: 'Browser Render',
        simple: "The browser reads the HTML, figures out what everything should look like, and paints pixels onto your screen — piece by piece as data arrives.",
        deep: "The critical rendering path: HTML to DOM, CSS to CSSOM, DOM plus CSSOM to Render Tree, Layout (geometry), Paint (pixels), Composite (GPU layers). Render-blocking scripts pause HTML parsing until they execute. Async and deferred scripts avoid this. First Contentful Paint (FCP) and Largest Contentful Paint (LCP) are the user-visible metrics that capture this pipeline.",
      },
    ],
    failures: [
      {
        name: 'DNS Lookup Failed',
        cause: 'Domain does not exist, is misspelled, or the configured DNS server is unreachable.',
        symptom: 'ERR_NAME_NOT_RESOLVED or DNS_PROBE_FINISHED_NXDOMAIN in Chrome. The browser never reaches TCP connect.',
        fix: 'Run `dig domain.com` to verify the record exists. Try `dig @8.8.8.8 domain.com` to test with Google DNS. Flush local DNS cache: `sudo dscacheutil -flushcache` on macOS.',
        severity: 'critical',
      },
      {
        name: 'TCP Connection Timeout',
        cause: 'Server is down, the IP is wrong, or a firewall is silently dropping packets to port 443.',
        symptom: 'ERR_CONNECTION_TIMED_OUT — the browser hangs for 30+ seconds then gives up. ping succeeds but the site never loads.',
        fix: 'Ping the IP to test L3 connectivity. Use `nc -zv host 443` to test if port 443 accepts connections. Check security group or iptables rules.',
        severity: 'critical',
      },
      {
        name: 'TLS Certificate Error',
        cause: 'Certificate is expired, issued for a different domain, or missing intermediate CA certificates in the chain.',
        symptom: 'NET::ERR_CERT_AUTHORITY_INVALID or NET::ERR_CERT_DATE_INVALID — red padlock warning that blocks most users.',
        fix: 'Run `openssl s_client -connect host:443 -servername host` to inspect the full certificate chain and expiry dates. Use ssllabs.com for a comprehensive audit.',
        severity: 'high',
      },
    ],
    commands: [
      {
        label: 'Time every layer',
        cmd: "curl -o /dev/null -s -w 'DNS: %{time_namelookup}s\\nTCP: %{time_connect}s\\nTLS: %{time_appconnect}s\\nTTFB: %{time_starttransfer}s\\nTotal: %{time_total}s\\n' https://example.com",
        what: 'Breaks down exact milliseconds spent at each layer — DNS, TCP, TLS, and server response. If TLS time is 0, TLS failed. If TTFB is 3s, the server is slow.',
      },
      {
        label: 'Trace DNS resolution',
        cmd: 'dig google.com +trace',
        what: 'Walks the full DNS tree from root servers down to the authoritative answer, showing every hop, TTL, and nameserver involved.',
      },
      {
        label: 'Test TCP port',
        cmd: 'nc -zv google.com 443',
        what: 'Tests whether a TCP port is open and accepting connections. Fails immediately if firewalled or server is down. Distinguishes port-blocked from server-unreachable.',
      },
    ],
    a: {
      v: '🍕📞🔐',
      t: 'The pizza delivery analogy',
      tx: "Think of loading a website like ordering pizza. First, you look up the restaurant's phone number — that's DNS, translating 'google.com' into an actual address the internet can use. Then you call and the phone rings three times before anyone picks up — that's the TCP handshake, making sure a real connection exists. Next, the restaurant checks your ID to make sure you're calling a real, legitimate place — that's TLS verifying the certificate. Then you place your order — that's the HTTP request. You wait while they cook — that's the server running code and querying databases. The delivery driver arrives with the pizza — that's the HTTP response sending back HTML. And finally you open the box and eat — that's the browser rendering the page you see.",
      s: "The performance insight: the browser request lifecycle is a chain of sequential steps, and each step cannot start until the previous one completes. DNS must finish before TCP can start. TCP must finish before TLS can start. TLS must finish before HTTP can start. This is why reducing round-trip time — by moving servers closer to users with a CDN — has a compounding effect: it cuts time off DNS, TCP, and TLS simultaneously.",
    },
    te: {
      def: 'The browser request lifecycle is the sequence of network operations — DNS, TCP, TLS, HTTP — that transforms a URL into rendered pixels. Each layer is a protocol that operates independently and hands off to the next.',
      types: [
        { n: 'HTTP/1.1', d: 'One request per connection. Keep-alive reuses connections but requests are still serial within a connection. Multiple connections per origin (typically 6) work around this.' },
        { n: 'HTTP/2', d: 'Multiplexed streams over one TLS connection. Eliminates head-of-line blocking at the HTTP layer. Header compression via HPACK reduces overhead.' },
        { n: 'HTTP/3 / QUIC', d: 'UDP-based transport with TLS 1.3 built in. Eliminates TCP head-of-line blocking. Faster connection setup. Better performance on lossy mobile networks.' },
      ],
      when: 'Understanding the lifecycle is essential for performance optimization: every CDN, TLS session cache, and HTTP/2 optimization targets a specific step in this chain.',
      trade: 'Speed vs. correctness: aggressive caching reduces DNS, TCP, and TLS overhead but risks serving stale content. TLS adds 1 RTT but is non-negotiable for security.',
      rw: {
        ex: ['Every page load in Chrome DevTools Network tab', 'API calls from a mobile app', 'Webhook delivery to external endpoints'],
        cs: "Google found that every 100ms of added latency reduces searches by 0.2% — a number that compounds to millions of dollars at their scale. The browser request lifecycle is the map of every place you can claw back those milliseconds: move DNS with low TTLs, terminate TLS at the edge, cache aggressively at every layer.",
      },
    },
    interview: {
      q: 'Walk me through what happens when you type a URL in the browser.',
      a: 'Starting from the keypress: browser checks cache, DNS resolves the domain (recursive resolver, root, TLD, authoritative), TCP three-way handshake establishes the connection, TLS 1.3 handshake negotiates encryption in 1 RTT, HTTP/2 GET request is sent with compressed headers, the server runs application code and queries the database, HTTP 200 response returns HTML with cache headers, and the browser parses HTML to DOM, CSS to CSSOM, combines into a render tree, runs layout, paints pixels, and composites layers.',
      fu: [
        'How would you minimize time to first byte?',
        'What changes with HTTP/3?',
        'Where does TLS termination happen in a production system?',
        'How does a CDN change this flow?',
      ],
    },
  },

  {
    id: 'dns-explained',
    cat: 'networking',
    color: '#6272d4',
    icon: '-',
    title: "How does the internet know where 'google.com' lives?",
    tag: "The phone book that runs the entire internet",
    hook: "There are over 350 million registered domain names. None of them are stored on your computer. Yet your browser finds any of them in under 50 milliseconds. A new domain, freshly registered, won't be found — not because the record doesn't exist, but because the answer is cached at every layer of the internet with an expiry time that hasn't elapsed yet. That's why your new site 'doesn't work yet' for up to 48 hours after you set up DNS. The system that makes this work is one of the most elegant pieces of distributed engineering ever built — and it hasn't fundamentally changed since 1983.",
    simpleExplain: "Imagine asking for a phone number in layers. You ask your mum — she checks her notes (OS cache). Mum doesn't know, so she calls grandma — grandma checks her address book (ISP resolver). Grandma doesn't know either, so she calls the national directory — they say 'for UK numbers, call the London office' (root server). Grandma calls the London office — they say 'for google.com, call Sarah's team' (TLD server). Grandma calls Sarah — Sarah gives the actual number (authoritative DNS). Grandma writes it down with an expiry date (TTL). Next time anyone asks grandma the same question, she answers instantly from her notes until the expiry passes.",
    howItWorks: "DNS resolution is a recursive lookup through a tree of specialized servers, each knowing only its own level of the hierarchy. The recursive resolver (typically your ISP's server, or a public one like 8.8.8.8) does the walking: it first queries a root nameserver to find which TLD server manages '.com', then queries the TLD server to find which nameservers are authoritative for 'google.com', then queries those authoritative nameservers for the actual A record. Each answer is cached at every level according to its TTL. A low TTL means resolvers re-query frequently — good for fast failover, bad for latency. A high TTL means resolvers cache longer — good for performance, bad for propagation speed when records change.",
    pipeline: [
      {
        icon: 'Monitor',
        label: 'Browser Cache',
        simple: "Did you visit this site recently? Your browser remembers the IP for a short time so it doesn't have to look it up every single time.",
        deep: "Chrome's DNS cache is visible at chrome://net-internals/#dns. Entries are bounded by TTL and expire automatically. A browser cache hit means zero network calls for DNS — the browser opens a TCP connection directly.",
      },
      {
        icon: 'Cpu',
        label: 'OS Cache',
        simple: "Does your computer itself remember the address from a recent visit?",
        deep: "The OS checks /etc/hosts first (which can override any DNS record), then its local resolver cache. `ipconfig /displaydns` on Windows shows the OS cache. TTL-bounded — entries expire without manual intervention.",
      },
      {
        icon: 'Wifi',
        label: 'Router Cache',
        simple: "Does your home router have the answer saved from a recent request by anyone on your network?",
        deep: "Most consumer routers run a lightweight DNS forwarder (dnsmasq) that caches responses for all devices on the network. A hit here saves a round-trip to the ISP resolver. Router caches are shared across all household devices.",
      },
      {
        icon: 'Globe',
        label: 'ISP Resolver',
        simple: "Your internet provider's lookup service takes over and does the hard work of walking the DNS hierarchy on your behalf.",
        deep: "The recursive resolver contacts root servers, TLD servers, and authoritative servers in sequence, caching each answer. Many users override their ISP's resolver with 8.8.8.8 (Google) or 1.1.1.1 (Cloudflare) for privacy and speed.",
      },
      {
        icon: 'Globe2',
        label: 'Root Server',
        simple: "Thirteen root server clusters worldwide know who's in charge of each domain ending — .com, .org, .io, and so on.",
        deep: "There are 13 anycast root clusters (a.root-servers.net through m.root-servers.net) backed by 1500+ physical servers worldwide. They don't store A records — they know which TLD servers manage each top-level domain. Root server answers are cached for 48 hours.",
      },
      {
        icon: 'MapPin',
        label: 'TLD Server',
        simple: "The .com server knows which nameserver is responsible for 'google.com' specifically.",
        deep: "Verisign operates the .com TLD nameservers. They hold NS records for every .com domain — pointing the resolver to Google's authoritative nameservers (ns1.google.com, ns2.google.com). TLD servers don't hold A records; they point to who does.",
      },
      {
        icon: 'Server',
        label: 'Authoritative DNS',
        simple: "Google's own DNS server finally gives the real IP address — the definitive answer.",
        deep: "The authoritative nameserver is the source of truth. It returns A records (IPv4) or AAAA records (IPv6) for the domain. This is what you configure when you add DNS records in your registrar or Route 53. The TTL on this answer controls how long resolvers cache it.",
      },
      {
        icon: 'ArrowRight',
        label: 'IP Returned',
        simple: "Your browser now has the address it needs and can start connecting to the server.",
        deep: "The IP propagates back through the resolver chain, cached at each level per its TTL value. Low TTL (60–300s) means fresh data but higher resolver load. High TTL (3600–86400s) means faster lookups but slower change propagation. Cloudflare and Google both use 300s for their own domains.",
      },
    ],
    failures: [
      {
        name: 'NXDOMAIN — Domain Not Found',
        cause: 'Domain does not exist, was mistyped, or the authoritative nameserver has no record for the queried hostname.',
        symptom: 'Browser shows DNS_PROBE_FINISHED_NXDOMAIN. ping also fails with "cannot resolve hostname".',
        fix: 'Run `dig +short domain.com` — if it returns nothing, the DNS record is missing. Check registrar DNS settings. Verify NS records point to the correct nameservers with `dig NS domain.com`.',
        severity: 'critical',
      },
      {
        name: 'DNS Propagation Delay',
        cause: 'A DNS record was recently changed but the old TTL has not yet expired at all resolvers worldwide.',
        symptom: 'Site works for some users but not others. Different IPs returned from different geographic locations. The DNS change looks correct at the authoritative level.',
        fix: 'Use whatsmydns.net to check propagation globally. Lower TTL to 60s at least 24 hours before planned DNS changes so the propagation window is short when you make the change.',
        severity: 'medium',
      },
      {
        name: 'DNS Hijacking',
        cause: 'An ISP, attacker, or malicious router is intercepting DNS responses and substituting incorrect IP addresses.',
        symptom: 'Site loads but with unexpected content, or redirects to a phishing page. The IP returned differs from what authoritative servers report.',
        fix: 'Compare `dig domain.com` with `dig @8.8.8.8 domain.com`. If they differ, your local resolver is hijacked. Switch to 1.1.1.1 or 8.8.8.8. Enable DNSSEC on your domain to cryptographically validate responses.',
        severity: 'high',
      },
    ],
    commands: [
      {
        label: 'Full DNS trace',
        cmd: 'dig google.com +trace',
        what: 'Walks the entire DNS tree from root servers to the authoritative answer, showing every hop, TTL, and nameserver consulted. Essential for diagnosing propagation issues.',
      },
      {
        label: 'Check what IP resolves to',
        cmd: 'dig +short google.com',
        what: "Returns just the IP address your DNS resolver returns for a domain. Fast sanity check — compare to the expected IP to detect stale caches or hijacking.",
      },
      {
        label: 'Query a specific DNS server',
        cmd: 'dig @1.1.1.1 google.com',
        what: "Asks Cloudflare's DNS (1.1.1.1) directly, bypassing your ISP's resolver. Useful to compare with your local resolver when debugging propagation delays.",
      },
    ],
    a: {
      v: '📞👩‍👧‍👦🔎',
      t: 'The phone book in layers',
      tx: "Imagine asking for a phone number in layers. You ask your mum — she checks her notes (OS cache). Mum doesn't know, so she calls grandma — grandma checks her address book (ISP resolver). Grandma doesn't know either, so she calls the national directory — they say 'for UK numbers, call the London office' (root server). Grandma calls the London office — they say 'for google.com, call Sarah's team' (TLD server). Grandma calls Sarah — Sarah gives the actual number (authoritative DNS). Grandma writes it down with an expiry date (TTL). Next time anyone asks grandma the same question, she answers instantly from her notes until the expiry passes.",
      s: "The operational insight: DNS TTL is not a timer that starts when you change a record. It's the maximum cache lifetime at each resolver that fetched the record before your change. If a resolver cached your record with TTL 86400 (24 hours) just one minute before you changed it, it won't re-query for another 23 hours 59 minutes. The professional move: lower your TTL to 60s at least 24 hours before any planned DNS change, so all existing cache entries expire within a minute of your change going live.",
    },
    te: {
      def: 'DNS (Domain Name System) is a hierarchical distributed database that translates human-readable hostnames into machine-routable IP addresses, using a tree of specialized servers cached at each level by TTL.',
      types: [
        { n: 'A Record', d: 'Maps a domain to an IPv4 address. The most common record type — what browsers use to open TCP connections.' },
        { n: 'AAAA Record', d: 'Maps a domain to an IPv6 address. IPv6 adoption is growing; many servers return both A and AAAA.' },
        { n: 'CNAME', d: 'Aliases one domain to another. The resolver follows the chain. Cannot be used at the apex domain (use ALIAS/ANAME instead).' },
        { n: 'MX Record', d: 'Specifies mail servers for the domain. Priority values determine which server is tried first.' },
      ],
      when: 'DNS TTL management is critical whenever you plan an IP migration, blue-green deployment, or CDN switch — the TTL you set today determines how fast your change propagates tomorrow.',
      trade: 'Low TTL enables fast failover but increases resolver query load and adds latency for cache misses. High TTL improves performance but means a DNS mistake propagates slowly and is hard to roll back quickly.',
      rw: {
        ex: ['Cloudflare DNS (1.1.1.1) — fastest public resolver, privacy-first', 'AWS Route 53 with health-check based failover routing', 'Split-horizon DNS — different IPs returned for internal vs external queries'],
        cs: "When Slack had an outage in 2022, the root cause was a DNS misconfiguration during a routine network change. 10 million users were cut off. Because Slack had planned their DNS TTL at 30 seconds, full propagation of the fix took under a minute once the record was corrected. Without that low TTL, the outage would have extended by hours.",
      },
    },
    interview: {
      q: "A user says 'my new website isn't working' — it was working for you. What's the most likely cause and how do you debug it?",
      a: "Most likely a DNS propagation delay. Their resolver has a cached NXDOMAIN or old IP that hasn't expired. First, ask them to run `dig +short domain.com` and compare the IP to the expected value. Then check whatsmydns.net to see which regions are still serving the old record. If it's a new site, verify the DNS records exist at the authoritative level with `dig @1.1.1.1 domain.com`. If propagation is the issue, the only fix is waiting for the TTL to expire — lesson learned: always lower TTL before making DNS changes.",
      fu: [
        "What's the difference between A, CNAME, and MX records?",
        'How does DNS TTL affect deployments?',
        'What is DNSSEC and why does it matter?',
        'How does Cloudflare use DNS to route traffic globally?',
      ],
    },
  },

  {
    id: 'osi-tcp',
    cat: 'networking',
    color: '#6272d4',
    icon: '-',
    title: "Why does the internet work in layers — like a burger?",
    tag: "The model that lets your phone call a server from 1969",
    hook: "When you send a message, you don't think about electrical signals, binary frames, or IP routing tables. You just type and hit send. That invisibility is engineered — and the OSI model is the blueprint that makes it work. Seven layers, each completely ignorant of the others, each doing exactly one job. Ethernet doesn't know about HTTP. IP doesn't know about TLS. TCP doesn't know what a web page is. This separation is what lets your 2024 smartphone talk to a server architecture designed in the 1960s — and why you can swap out Wi-Fi for fiber without changing a single line of application code.",
    simpleExplain: "Think of sending a physical letter. You write the message (Application layer). You fold it and put it in an envelope — presentation. You write a return address — session management. You write the destination address on the envelope — network layer. You drop it in your mailbox — data link, handing it to the local carrier. Postal trucks drive it across the country — physical layer, actual transport. Each person in the chain does only their one job: the truck driver doesn't read your letter, the post office doesn't care what language it's in. The OSI model is this same separation of concerns, applied to network protocols.",
    howItWorks: "When your application sends data, it passes down through each OSI layer, and each layer wraps it in its own header (encapsulation). The Application layer creates an HTTP request. The Transport layer (TCP) adds source and destination ports, sequence numbers, and checksum — creating a segment. The Network layer (IP) adds source and destination IP addresses — creating a packet. The Data Link layer (Ethernet) adds MAC addresses and a frame check — creating a frame. The Physical layer converts the frame to electrical signals or photons. On the receiving side, each layer strips its header and passes the payload up. A router only needs to implement layers 1–3. A switch only needs layers 1–2. Your browser only needs layer 7.",
    pipeline: [
      {
        icon: 'Monitor',
        label: 'Application (L7)',
        simple: "The actual app talking — your browser making an HTTP request, your email client sending SMTP, your game sending UDP packets.",
        deep: "HTTP, DNS, SMTP, FTP, WebSocket. This layer defines what the data means and how to format it. When you hit Enter in Chrome, this layer formats an HTTP/2 GET request with headers. All the application-level protocols you interact with live here.",
      },
      {
        icon: 'Code2',
        label: 'Presentation (L6)',
        simple: "Translates data so both sides understand it — like a universal translator handling different languages and compression.",
        deep: "Handles character encoding (UTF-8, ASCII), data compression (gzip, brotli), and encryption (TLS in the OSI model lives conceptually here). In practice, the Presentation and Session layers are rarely implemented as distinct components — TLS bundles both.",
      },
      {
        icon: 'Layers',
        label: 'Session (L5)',
        simple: "Manages the conversation — it knows when a 'session' starts, how long it lasts, and when it ends.",
        deep: "Manages sessions: opening, maintaining checkpoints, and closing connections. HTTP keep-alive connections and TCP connection state map here conceptually. In real implementations, TCP handles much of what the OSI Session layer specifies.",
      },
      {
        icon: 'Route',
        label: 'Transport (L4)',
        simple: "Guarantees your data arrives complete, in order, and with nothing missing — like a signed-for delivery service.",
        deep: "TCP: reliable, ordered, flow-controlled, connection-oriented. Splits data into segments, assigns sequence numbers, retransmits on loss. UDP: fire-and-forget, no ordering, no retransmit — minimal overhead. Port numbers live here: HTTP=80, HTTPS=443, SSH=22. Load balancers operating at L4 route by IP:port without reading the HTTP payload.",
      },
      {
        icon: 'Globe',
        label: 'Network (L3)',
        simple: "Figures out the best path across the entire internet to get your data from your machine to a server anywhere in the world.",
        deep: "The IP protocol. Every packet carries source and destination IP addresses. Routers operate exclusively at L3 — they read the destination IP, consult their routing table, and forward the packet one hop closer to the destination. BGP is the protocol routers use to share routing information with each other.",
      },
      {
        icon: 'Network',
        label: 'Data Link (L2)',
        simple: "Moves data between two directly-connected devices — your laptop to your router, or one switch to the next.",
        deep: "Ethernet frames and MAC addresses. Switches operate at L2 — they maintain a MAC address table and forward frames to the correct port. ARP maps IP addresses to MAC addresses on a local network. This layer only handles one hop at a time; IP handles routing across many hops.",
      },
      {
        icon: 'Activity',
        label: 'Physical (L1)',
        simple: "The actual cable, Wi-Fi radio waves, or fiber optic light pulses carrying raw bits from one device to the next.",
        deep: "Electrical signals (Ethernet copper), radio waves (Wi-Fi 802.11), light pulses (fiber optic). Defines bit rates, voltage levels, connector pinouts, and modulation schemes. No addressing at all — just converting frames into signals and back. Swapping Wi-Fi for Ethernet changes only this layer; everything above continues unchanged.",
      },
    ],
    failures: [
      {
        name: 'Packet Loss (L3/L4)',
        cause: 'Network congestion causes router buffer overflow, dropping packets. Faulty hardware or duplex mismatch also causes loss.',
        symptom: 'Slow or intermittent connections, high and variable ping times, TCP retransmissions visible in Wireshark as black rows.',
        fix: 'Use `ping -c 100 host` to measure loss percentage. Run traceroute to identify which hop drops packets. Check for duplex mismatch at L2 with `ethtool eth0`. On cloud: check security group stateful rules.',
        severity: 'high',
      },
      {
        name: 'Port Blocked (L4)',
        cause: 'Firewall rule (iptables, security group, or hardware firewall) is blocking a specific TCP or UDP port.',
        symptom: 'Connection times out rather than refusing. telnet or nc to the port hangs but the server is pingable. Only specific ports fail.',
        fix: '`nc -zv host port` or `telnet host port` to test port reachability. Check iptables rules (`iptables -L -n`) and cloud security group inbound rules. Verify the app is actually listening on that port with `ss -tulpn`.',
        severity: 'high',
      },
      {
        name: 'MTU Mismatch (L3)',
        cause: "Packet size exceeds a link's MTU (typically 1500 bytes on Ethernet). If ICMP fragmentation-needed messages are blocked, packets are silently dropped.",
        symptom: 'Small packets and ping work fine. Large data transfers (HTTPS, SSH file copy) hang or time out. Often seen with VPNs.',
        fix: '`ping -s 1472 host` to test if large packets survive. Set MTU to 1400 on VPN interfaces. Enable PMTU Discovery. Check that ICMP type 3 code 4 (fragmentation needed) is not blocked by firewalls.',
        severity: 'medium',
      },
    ],
    commands: [
      {
        label: 'Trace every network hop',
        cmd: 'traceroute google.com',
        what: 'Shows every router your packets pass through, with latency per hop. Pinpoints exactly where slowness or packet loss begins in the network path.',
      },
      {
        label: 'Capture live packets',
        cmd: 'tcpdump -i any -n host google.com',
        what: 'Captures all packets to/from google.com. Shows the raw TCP handshake, HTTP request bytes, and response at the packet level. Essential for diagnosing L4 problems.',
      },
      {
        label: 'Check open ports on your machine',
        cmd: 'ss -tulpn',
        what: 'Lists all listening TCP and UDP sockets with their ports and the processes using them. Linux replacement for netstat — faster and more informative.',
      },
    ],
    a: {
      v: '✉️🚚📦',
      t: 'Sending a letter through the postal system',
      tx: "Think of sending a physical letter. You write the message (Application layer). You fold it and put it in an envelope — presentation. You write a return address — session management. You write the destination address on the envelope — network layer. You drop it in your mailbox — data link, handing it to the local carrier. Postal trucks drive it across the country — physical layer, actual transport. Each person in the chain does only their one job: the truck driver doesn't read your letter, the post office doesn't care what language it's in. The OSI model is this same separation of concerns, applied to network protocols.",
      s: "The engineering insight: because each layer only communicates with the layer directly above and below it, you can swap out implementations at any layer without touching the others. That's why HTTP/3 replaced TCP (L4) with QUIC without changing the HTTP protocol (L7), and why your app doesn't need to change when you move from Wi-Fi to Ethernet. Layers are contracts, not implementations.",
    },
    te: {
      def: 'The OSI (Open Systems Interconnection) model is a 7-layer conceptual framework that standardizes how different network protocols interact — from physical electrical signals at L1 to application data at L7.',
      types: [
        { n: 'TCP', d: 'Reliable, ordered delivery. SYN/ACK handshake establishes connection. Used for HTTP, databases, SSH. Adds one RTT latency for connection setup.' },
        { n: 'UDP', d: 'Unreliable, no ordering, no handshake. Low overhead. Used for DNS, video streaming, online games, and VoIP where some loss is acceptable.' },
        { n: 'QUIC', d: 'UDP-based transport with TLS 1.3 built in. Used by HTTP/3. Eliminates TCP head-of-line blocking. Faster connection setup than TCP+TLS separately.' },
      ],
      when: "Understanding OSI layers is essential for debugging: 'ping works but HTTPS doesn't' tells you the problem is at L4 or above. 'Can't ping but traceroute shows 3 hops' tells you L3 is partially working.",
      trade: 'TCP provides reliability at the cost of latency (retransmissions, head-of-line blocking). UDP provides speed at the cost of reliability. QUIC tries to get the benefits of both.',
      rw: {
        ex: ['Wireshark packet analyzer — inspects every layer of every packet', 'iptables firewall rules operate at L3/L4', 'AWS NLB (L4 load balancer) vs ALB (L7 load balancer)'],
        cs: "When engineers designed HTTP/3, they moved from TCP to UDP (QUIC) specifically to fix TCP's head-of-line blocking — a consequence of TCP's L4 guarantee that packets are delivered in order. One lost UDP packet doesn't block other QUIC streams, whereas one lost TCP segment blocks the entire TCP connection. Understanding OSI layers tells you exactly why this tradeoff exists and why the fix required changing the transport layer.",
      },
    },
    interview: {
      q: 'A service is reachable by ping but HTTPS connections time out. What layer do you suspect and how do you debug?',
      a: "Ping (ICMP) succeeds means L3 (IP routing) is working. HTTPS fails means the problem is at L4 (port 443 blocked by firewall), L4 (nothing listening on 443), or L6/L7 (TLS failure). First: `nc -zv host 443` to test TCP connectivity. If it hangs, a firewall is blocking port 443. If it refuses, nothing is listening. If it connects, try `openssl s_client -connect host:443` to test TLS. Also check if the server process is running and listening with `ss -tulpn | grep 443`.",
      fu: [
        "What's the difference between TCP and UDP and when would you choose each?",
        'How does a load balancer operate at L4 vs L7?',
        'What is a three-way handshake and why does TCP need it?',
      ],
    },
  },

  {
    id: 'https-tls',
    cat: 'networking',
    color: '#6272d4',
    icon: '-',
    title: "What does that padlock in your browser actually mean?",
    tag: "How two strangers agree on a secret in public",
    hook: "HTTP sends everything as plain text. Without HTTPS, someone on your coffee shop Wi-Fi could watch network traffic and read your passwords, credit card numbers, and private messages in real time — no hacking required, just a packet capture. HTTPS prevents this using a handshake so mathematically elegant that two complete strangers can establish a shared secret in full public view, without ever transmitting the secret itself. The padlock icon is the browser telling you: this happened, and it worked.",
    simpleExplain: "Imagine you want to pass a secret note to a friend in a room full of people you don't trust. You can't whisper it across the room. So instead, you and your friend both bring padlocks. You open a box, lock it with their padlock, and slide the box across the room. Only they have the key to their padlock — so only they can open it. Now you both have a shared secret. Nobody watching learned anything, because the secret never crossed the room in the open. TLS does exactly this, mathematically, in about 50 milliseconds.",
    howItWorks: "TLS 1.3 reduces the handshake to a single round-trip. The client sends ClientHello containing its supported cipher suites and its ECDHE key share. The server selects a cipher suite, sends its own ECDHE key share, its certificate, and a Finished message. Both sides independently compute the same shared secret using elliptic curve math: they each have each other's public key, and the math guarantees they arrive at the same private secret without transmitting it. All subsequent communication uses symmetric AES-256-GCM encryption — which is orders of magnitude faster than the asymmetric crypto used in the handshake. Perfect forward secrecy means each session uses fresh ephemeral keys, so compromising the server's private key in the future doesn't decrypt past sessions.",
    pipeline: [
      {
        icon: 'Monitor',
        label: 'Client Hello',
        simple: "Your browser announces 'I want a secure connection' and shares all the encryption methods it supports.",
        deep: "ClientHello sends the TLS version (1.3 preferred with 1.2 fallback), a list of supported cipher suites (e.g., TLS_AES_256_GCM_SHA384), and a key_share extension with the client's ECDHE public key. It also sends SNI (Server Name Indication) — the hostname — so one server can handle TLS for multiple domains on the same IP.",
      },
      {
        icon: 'Server',
        label: 'Server Hello',
        simple: "The server picks the best security settings, proves who it is by sending its certificate, and sends its half of the secret-key exchange.",
        deep: "ServerHello picks the cipher suite and sends the server's ECDHE key share. In the same flight, the server also sends Certificate (its TLS certificate signed by a CA), CertificateVerify (a signature with its private key proving it owns the cert), and Finished (a MAC of the entire handshake). All in one round trip in TLS 1.3.",
      },
      {
        icon: 'Shield',
        label: 'Certificate Check',
        simple: "Your browser verifies: is this certificate real? Was it issued by someone trustworthy? Is it for this domain? Has it expired?",
        deep: "The browser builds the certificate chain up to a trusted Root CA pre-installed in the OS trust store. It checks validity dates, that the Subject Alternative Name (SAN) matches the domain, and optionally checks OCSP for revocation. Self-signed certs fail because no trusted CA signed them. Certificate Transparency (CT) logs are also checked — all public certs must appear in them.",
      },
      {
        icon: 'Key',
        label: 'Key Exchange',
        simple: "Both sides independently do the same math to arrive at the same secret encryption key — without ever sending the key across the wire.",
        deep: "ECDHE (Elliptic Curve Diffie-Hellman Ephemeral): both sides have a public+private key pair on the same elliptic curve. Using point multiplication, each side computes: their_private times other_public = shared_secret. Due to the elliptic curve discrete logarithm problem, an attacker who sees both public keys cannot derive the shared secret. Ephemeral means a fresh key pair is generated per session — enabling perfect forward secrecy.",
      },
      {
        icon: 'Lock',
        label: 'Handshake Complete',
        simple: "Both sides confirm they've agreed on the same secret. The secure channel is now open.",
        deep: "Both sides send Finished messages, encrypted with the derived handshake keys, that MAC the entire transcript. This confirms neither side has been tampered with (integrity) and that they derived the same keys (consistency). The handshake took 1 RTT in TLS 1.3. For returning clients, 0-RTT resumption can send application data with the first message.",
      },
      {
        icon: 'ArrowRight',
        label: 'Encrypted Data',
        simple: "Everything from now on is scrambled — only you and the server can read it. Not your ISP, not your router, not anyone on the network.",
        deep: "Symmetric AES-256-GCM encryption for all application data. AEAD (Authenticated Encryption with Associated Data) provides both confidentiality and tamper-detection in a single operation. Record-level authentication tags mean any modification to a TLS record causes decryption to fail, protecting against active MITM attacks.",
      },
    ],
    failures: [
      {
        name: 'Certificate Expired',
        cause: "The TLS certificate was not renewed before its NotAfter date. Let's Encrypt certs expire every 90 days.",
        symptom: 'NET::ERR_CERT_DATE_INVALID. Red padlock. Affects all users immediately and completely — there is no grace period.',
        fix: 'Run `openssl s_client -connect host:443 | openssl x509 -noout -dates` to check expiry. Renew with `certbot renew`. Set up auto-renewal cron: `0 0 * * * certbot renew --quiet`. Monitor cert expiry with an uptime tool.',
        severity: 'critical',
      },
      {
        name: 'Mixed Content Warning',
        cause: 'An HTTPS page loads resources (images, scripts, API calls) over HTTP instead of HTTPS.',
        symptom: "Padlock shows a warning triangle. Modern browsers silently block mixed active content (scripts). Console shows 'Mixed Content: The page was loaded over HTTPS, but requested an insecure resource'.",
        fix: "Search codebase for hardcoded http:// asset URLs and update to https:// or protocol-relative //. Check browser DevTools Network tab filtering by 'mixed-content'. Use a CSP header with upgrade-insecure-requests.",
        severity: 'medium',
      },
      {
        name: 'Certificate Chain Incomplete',
        cause: 'The server is only sending the leaf certificate without the intermediate CA certificates needed to build the chain to a trusted root.',
        symptom: 'Works in Chrome (which uses AIA fetching to download missing intermediates) but fails in curl, mobile apps, and older clients with SSL_ERROR_BAD_CERT_DOMAIN or UNABLE_TO_VERIFY_LEAF_SIGNATURE.',
        fix: "Run `openssl s_client -connect host:443 -showcerts` and count certificates in the chain. Configure your web server to use the full chain file (fullchain.pem in Let's Encrypt). Audit at ssllabs.com.",
        severity: 'high',
      },
    ],
    commands: [
      {
        label: 'Inspect TLS certificate',
        cmd: 'openssl s_client -connect google.com:443 -servername google.com < /dev/null | openssl x509 -noout -subject -dates -issuer',
        what: 'Shows certificate subject, validity dates, and issuing CA without a browser. Instant diagnosis for cert errors — runs from any server or CI environment.',
      },
      {
        label: 'Test TLS handshake speed',
        cmd: "curl -o /dev/null -s -w 'TLS handshake: %{time_appconnect}s\\n' https://google.com",
        what: 'Shows exactly how long the TLS handshake took in isolation, separate from DNS and TCP. Typically 50–150ms. High values indicate server-side TLS performance issues.',
      },
      {
        label: 'Full SSL audit',
        cmd: 'docker run --rm drwetter/testssl.sh google.com',
        what: 'Comprehensive TLS audit — cipher suites, protocol versions, certificate chain completeness, HSTS, and known vulnerabilities (Heartbleed, POODLE, ROBOT). Industry-standard tool.',
      },
    ],
    a: {
      v: '📦🔐🗝️',
      t: 'The padlock box analogy',
      tx: "Imagine you want to pass a secret note to a friend in a room full of people you don't trust. You can't whisper it across the room. So instead, you and your friend both bring padlocks. You open a box, lock it with their padlock, and slide the box across the room. Only they have the key to their padlock — so only they can open it. Now you both have a shared secret. Nobody watching learned anything, because the secret never crossed the room in the open. TLS does exactly this, mathematically, in about 50 milliseconds.",
      s: "The key insight about perfect forward secrecy: TLS 1.3 uses ephemeral key pairs (new ones generated for every session) rather than the server's long-term private key for the actual key exchange. This means if an attacker records encrypted traffic today and steals the server's private key five years from now, they still cannot decrypt today's traffic — because the ephemeral keys used for the key exchange were discarded immediately after the handshake.",
    },
    te: {
      def: 'TLS (Transport Layer Security) is a cryptographic protocol that provides authentication (certificate verification), confidentiality (encryption), and integrity (tamper detection) for data transmitted over a network.',
      types: [
        { n: 'TLS 1.3', d: 'Current standard (2018). 1-RTT handshake, removes weak ciphers (RC4, MD5, SHA-1), mandatory forward secrecy via ECDHE, encrypted certificate transmission.' },
        { n: 'mTLS', d: 'Mutual TLS — both client and server present and verify certificates. Used for service-to-service authentication in microservices and zero-trust architectures.' },
        { n: 'HSTS', d: "HTTP Strict Transport Security — a response header instructing browsers to always use HTTPS for this domain, preventing SSL stripping downgrade attacks. max-age=31536000 is recommended." },
      ],
      when: 'TLS is non-negotiable for any production system handling authentication, payments, or personal data. mTLS is used when you need to verify client identity at the transport level, not just user identity at the application level.',
      trade: 'TLS adds 1 RTT of handshake latency and CPU overhead for crypto operations. TLS 1.3 minimizes this with 1-RTT handshakes and hardware-accelerated AES. The security guarantee justifies the overhead in virtually every case.',
      rw: {
        ex: ["Let's Encrypt — free automated cert issuance trusted by all major browsers", 'Cloudflare terminates TLS at the edge for millions of origins', 'mTLS in Istio and Linkerd service meshes — automatic mutual auth between all services'],
        cs: "In 2011, DigiNotar (a Dutch Certificate Authority) was compromised. Attackers issued fraudulent TLS certificates for google.com, enabling man-in-the-middle attacks against Iranian dissidents. The CA was subsequently blacklisted by all browser vendors. This event directly accelerated Certificate Transparency (CT logs) — now every publicly-trusted certificate must be logged in an append-only public ledger before browsers will accept it.",
      },
    },
    interview: {
      q: 'Explain how HTTPS protects data in transit and what happens during the TLS handshake.',
      a: "HTTPS is HTTP over TLS. TLS provides three guarantees: authentication (the server's certificate proves its identity), confidentiality (data is encrypted with AES-256-GCM), and integrity (AEAD tags detect tampering). The TLS 1.3 handshake: client sends ClientHello with supported ciphers and its ECDHE key share. Server responds with ServerHello (cipher choice, its ECDHE key share), Certificate, CertificateVerify, and Finished — all in one RTT. Both sides derive the same shared secret via ECDHE math without transmitting it. All subsequent data uses symmetric AES encryption. Perfect forward secrecy means ephemeral keys are discarded after each session.",
      fu: [
        'What is perfect forward secrecy and why does it matter?',
        'How does certificate pinning work?',
        "What's the difference between TLS 1.2 and TLS 1.3?",
        "How does Let's Encrypt issue certificates automatically?",
      ],
    },
  },

  {
    id: 'reverse-proxy',
    cat: 'networking',
    color: '#6272d4',
    icon: '-',
    title: "Your request never actually reaches your code. Who intercepts it?",
    tag: "The invisible middleman running every production server",
    hook: "You deployed your app on port 3000. Users connect on port 443. Between those two ports sits a piece of software that almost every production system in the world depends on — it reads every request before your app sees it, terminates TLS so your app never touches a certificate, rate-limits abusers before they consume resources, routes traffic across multiple instances for load balancing, compresses responses to save bandwidth, and adds security headers your app forgot. It has no UI. You've never clicked it. But it's running right now on every major site you've used today. That's the reverse proxy.",
    simpleExplain: "Think of a high-end restaurant. When you walk in, you don't walk directly into the kitchen. The maitre d' greets you at the door, checks your reservation, decides which section of the restaurant you're seated in, and ensures you're assigned the right waiter for your table. Your application code is the kitchen — it does the actual cooking. Nginx is the maitre d'. Users never interact with the kitchen directly, and the kitchen doesn't have to worry about seating, reservations, or tipping. Each side does exactly one job.",
    howItWorks: "Nginx sits between the public internet and your application servers. It accepts incoming TCP connections on port 443, terminates TLS (decrypting requests), reads the Host header and URL path, and applies a set of rules: rate limiting by IP, access control, header manipulation. It then opens a new HTTP connection to the appropriate upstream server (your app on localhost:3000) and forwards the request — adding X-Real-IP and X-Forwarded-For headers so the app knows the original client IP. When the upstream responds, Nginx buffers the response, applies compression (gzip or brotli), adds configured response headers (HSTS, X-Frame-Options), and forwards the result to the client. Your application never handles TLS, compression, or rate limiting — the proxy does.",
    pipeline: [
      {
        icon: 'Monitor',
        label: 'User Browser',
        simple: "The user types yourdomain.com and their browser initiates a connection to port 443.",
        deep: "Browser initiates TCP SYN to the server's public IP on port 443. The Host header in the subsequent HTTP request tells Nginx which virtual host configuration to use — enabling multiple domains on one IP address.",
      },
      {
        icon: 'Globe',
        label: 'DNS Resolution',
        simple: "The domain name is translated to the server's IP address — or the CDN's IP if Cloudflare is in front.",
        deep: "If Cloudflare is used, DNS returns Cloudflare's anycast IP, not your server's IP directly. Cloudflare handles TLS termination at its edge and forwards plain HTTP (or re-encrypted HTTPS) to your origin Nginx. Your origin Nginx never sees the raw user TLS connection.",
      },
      {
        icon: 'Shield',
        label: 'Nginx (Reverse Proxy)',
        simple: "Nginx catches every request first — before your app ever sees it. It handles TLS, checks rate limits, and decides where to send the request.",
        deep: "Nginx terminates TLS (reads the encrypted request, decrypts it using the server certificate and private key), reads HTTP headers and URI, applies rate limiting (limit_req_zone), access control (allow/deny), and security headers. All this happens without your application code being involved.",
      },
      {
        icon: 'Network',
        label: 'Upstream Selection',
        simple: "Nginx picks which app server instance should handle this specific request.",
        deep: "Load balancing algorithms: round-robin (default, equal distribution), least-connections (routes to least-busy upstream), ip-hash (sticky sessions — same client always hits same upstream), weighted (send 80% to v2, 20% to v1 for canary). Passive health checks remove upstreams that return errors.",
      },
      {
        icon: 'Server',
        label: 'App Server',
        simple: "Your actual code finally runs — Node.js, Python, Go — and processes the business logic.",
        deep: "The app receives a plain HTTP request on localhost:3000 (or a Unix socket, which is faster than TCP for local IPC). X-Real-IP header carries the original client IP (not Nginx's 127.0.0.1). X-Forwarded-Proto tells the app it was originally HTTPS, so it doesn't issue HTTP redirects.",
      },
      {
        icon: 'Database',
        label: 'Database',
        simple: "Your app fetches the data it needs from the database to build its response.",
        deep: "DB queries happen here, behind the app server. Connection pooling (PgBouncer for Postgres, HikariCP for JVM apps) is critical — a DB accepts limited concurrent connections, but Nginx can route thousands of concurrent requests to multiple app instances sharing a pool.",
      },
      {
        icon: 'ArrowLeft',
        label: 'Response Path',
        simple: "The response travels back through Nginx, which adds security headers and compresses it before sending to the user.",
        deep: "Nginx adds headers (Strict-Transport-Security, X-Content-Type-Options, X-Frame-Options, Content-Security-Policy), applies gzip or brotli compression (typically 60–80% size reduction for HTML/JSON), and forwards over the TLS connection to the client. Connection is kept alive for subsequent requests without a new TLS handshake.",
      },
    ],
    failures: [
      {
        name: '502 Bad Gateway',
        cause: 'Nginx reached the upstream address but received an invalid response, or the upstream process crashed and the connection was refused.',
        symptom: "Users see '502 Bad Gateway'. Nginx error log shows 'connect() failed (111: Connection refused)' or 'upstream sent invalid header'.",
        fix: 'Check if app process is running: `ps aux | grep node` or `systemctl status your-app`. Check app logs for crash stack traces. Verify the upstream address in nginx.conf (`proxy_pass http://localhost:3000`) matches the port the app is actually listening on (`ss -tulpn | grep 3000`).',
        severity: 'critical',
      },
      {
        name: '504 Gateway Timeout',
        cause: 'The upstream app took longer than the configured proxy_read_timeout to respond. Default is 60 seconds.',
        symptom: "'504 Gateway Timeout'. Nginx access log shows the request duration exceeding the timeout. Often happens on database-heavy endpoints under load.",
        fix: 'Increase `proxy_read_timeout 120s;` in nginx.conf as a temporary fix. Permanently fix by profiling the slow endpoint — add DB query logging, use EXPLAIN ANALYZE on slow queries, add Redis caching for expensive computations.',
        severity: 'high',
      },
      {
        name: 'Real IP Showing as 127.0.0.1',
        cause: 'The application reads REMOTE_ADDR (the TCP connection source) instead of the X-Forwarded-For or X-Real-IP header set by Nginx.',
        symptom: 'All requests appear to originate from localhost in logs. IP-based rate limiting, geo-blocking, and analytics show 127.0.0.1 for all users.',
        fix: "Configure Nginx: `proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;`. In your app, read `req.headers['x-real-ip']` or use a middleware that sets the real IP from X-Forwarded-For (e.g., Express's `trust proxy` setting).",
        severity: 'medium',
      },
    ],
    commands: [
      {
        label: 'Test Nginx config before reload',
        cmd: 'nginx -t',
        what: 'Validates nginx.conf syntax without restarting. Always run before `nginx -s reload` — a syntax error in the new config would take down the server entirely if applied.',
      },
      {
        label: 'Watch Nginx access logs live',
        cmd: 'tail -f /var/log/nginx/access.log',
        what: 'Streams live requests in real time — shows client IP, HTTP method, URL, status code, response size, and response time. Essential for watching traffic during a deploy or incident.',
      },
      {
        label: 'Check which process owns port 443',
        cmd: "ss -tulpn | grep ':443'",
        what: 'Shows which process is listening on port 443. If Nginx fails to start, something else may have claimed the port. Also useful to verify Nginx is actually running after a service restart.',
      },
    ],
    a: {
      v: '🍽️👔🍳',
      t: "The restaurant maitre d'",
      tx: "Think of a high-end restaurant. When you walk in, you don't walk directly into the kitchen. The maitre d' greets you at the door, checks your reservation, decides which section of the restaurant you're seated in, and ensures you're assigned the right waiter for your table. Your application code is the kitchen — it does the actual cooking. Nginx is the maitre d'. Users never interact with the kitchen directly, and the kitchen doesn't have to worry about seating, reservations, or tipping. Each side does exactly one job.",
      s: "The operational insight: the reverse proxy is where cross-cutting concerns live. Rate limiting, TLS termination, compression, security headers, logging, health checks — none of these belong in your application code, and putting them there means reimplementing them in every service. A single well-configured Nginx handles all of this transparently for every upstream behind it, regardless of what language or framework those upstreams use.",
    },
    te: {
      def: 'A reverse proxy is a server that sits between clients and upstream services, forwarding requests on their behalf — handling TLS termination, routing, load balancing, and cross-cutting concerns without the upstream services being aware of client details.',
      types: [
        { n: 'Nginx', d: 'Event-loop async architecture. High throughput, low memory. De facto standard for web traffic proxying and static file serving.' },
        { n: 'HAProxy', d: 'TCP and HTTP load balancer. Industry-standard for high-throughput L4/L7 routing. Extremely precise health checks and connection stats.' },
        { n: 'Envoy', d: 'Cloud-native L7 proxy used in Istio and Consul service meshes. Dynamic configuration via xDS API. Emits rich telemetry for observability.' },
        { n: 'Caddy', d: "Automatic TLS via Let's Encrypt with zero configuration. Simpler config than Nginx. Ideal for small deployments." },
      ],
      when: "Use a reverse proxy in front of every production application. Even with a single app instance, the proxy handles TLS termination, security headers, and graceful deploys. With multiple instances, it also provides load balancing and health-based routing.",
      trade: 'Adds one hop to every request (microseconds of overhead, negligible in practice). The operational benefits — TLS offload, centralized logging, rate limiting, zero-downtime deploys — far outweigh this.',
      rw: {
        ex: ['Every major web server stack (Nginx in front of Node, Python, Ruby)', 'Kubernetes Ingress controllers (Nginx Ingress, Traefik)', 'API Gateway pattern in microservices'],
        cs: "Pinterest serves 100M+ users with a tiered Nginx setup: edge Nginx for TLS termination and caching, internal Nginx for service routing between Python microservices. By terminating TLS at the edge, their application services never touch certificate management — a single team owns the proxy config for all services.",
      },
    },
    interview: {
      q: 'What is a reverse proxy and how does it differ from a load balancer?',
      a: "A reverse proxy sits between clients and upstream servers, forwarding requests on behalf of the servers. It handles cross-cutting concerns: TLS termination, header manipulation, rate limiting, compression, security headers. A load balancer is a specific function that distributes requests across multiple upstreams — but it's typically one feature of a reverse proxy. Nginx does both. The key distinction: a forward proxy acts on behalf of clients (e.g., corporate proxy for outbound traffic), while a reverse proxy acts on behalf of servers. Load balancers can operate at L4 (routing by IP:port, no HTTP awareness) or L7 (routing by URL path, Host header, cookie — requires reading the HTTP payload).",
      fu: [
        'What is SSL termination and why would you terminate at the proxy rather than the app?',
        'How do you achieve zero-downtime deploys with Nginx?',
        "What's the difference between Nginx and a service mesh like Istio?",
        'How do you prevent a slow upstream from exhausting Nginx worker connections?',
      ],
    },
  },

  {
    id: 'cdn-explained',
    cat: 'networking',
    color: '#6272d4',
    icon: '-',
    title: "Why does a site in California load instantly in Tokyo?",
    tag: "Hidden copies of the internet, scattered across 300 cities",
    hook: "Physics says data cannot travel faster than light. The distance from New York to Tokyo is 10,900 kilometers — that's a minimum of 73 milliseconds at the speed of light, before any server processing, before any TLS handshake, before a single byte of your application runs. Yet Netflix streams in milliseconds from the other side of the planet. The solution is almost absurd in its elegance: instead of making data travel faster, make it travel less. Put a copy of your content 5 kilometers from every user on Earth. That's a CDN — and it's the reason any large-scale site feels local no matter where you are.",
    simpleExplain: "Imagine you run a bookshop with one warehouse in New York. Customers worldwide want your books. Shipping from New York to Japan takes two weeks. So instead, you print copies of your bestsellers and stock them in 300 local bookshops around the world. A customer in Tokyo doesn't wait for New York — they walk to their local Tokyo bookshop. CDNs do exactly this for websites: Cloudflare copies your CSS, JavaScript, images, and sometimes entire HTML pages to 300+ cities worldwide. Users get served from whichever city is closest to them, often in single-digit milliseconds.",
    howItWorks: "A CDN works by intercepting DNS — when you add a domain to Cloudflare, they give you new nameservers, and DNS now returns Cloudflare's anycast IP instead of your origin server's IP. Anycast routing means the same IP is announced from hundreds of locations, and BGP automatically routes each user to the topologically nearest PoP (Point of Presence). At the PoP, Cloudflare checks its cache for the requested URL. On a cache hit, it responds immediately without touching your origin. On a cache miss, it fetches from your origin, caches the response (respecting Cache-Control headers), and serves it. The next 10,000 users in that city get the cached response — your origin only handled the first request. This makes a CDN simultaneously a performance tool, a scaling tool, and a DDoS protection tool.",
    pipeline: [
      {
        icon: 'Monitor',
        label: 'User in Tokyo',
        simple: "A user in Tokyo types your URL. Their browser starts looking up where to connect.",
        deep: "DNS resolution begins. Without Cloudflare, the A record points to your California origin — the user's TCP connection travels 9,000km. With Cloudflare, the A record is Cloudflare's anycast IP — the same IP is announced from Tokyo's PoP, and BGP routes the connection there instead.",
      },
      {
        icon: 'Globe',
        label: 'Anycast DNS',
        simple: "Cloudflare's special routing system automatically sends users to the nearest Cloudflare city — Tokyo users go to Tokyo, not California.",
        deep: "Anycast assigns the same IP address to multiple physical nodes. BGP routes packets based on shortest AS path — so a Tokyo user's packets naturally route to Cloudflare's Tokyo PoP without any configuration. There's no DNS geo-targeting required.",
      },
      {
        icon: 'Zap',
        label: 'Tokyo Edge Node',
        simple: "Cloudflare's Tokyo server catches the request. It's only 5–10ms away from the user instead of 150ms.",
        deep: "Cloudflare's 330+ PoPs each run Nginx, a cache layer, DDoS scrubbing, WAF rules, and Workers (edge compute). The user completes the TCP handshake and TLS negotiation with Tokyo — 3 round trips of 5ms each instead of 150ms each. This alone saves 400ms+ per page load.",
      },
      {
        icon: 'Search',
        label: 'Cache Check',
        simple: "The Tokyo server asks: do I already have a fresh copy of this page? If yes, the response is sent immediately.",
        deep: "Cache key: URL plus Vary headers. Cache-Control headers from the origin response determine TTL. A `cf-cache-status: HIT` header in the response means Tokyo served from cache without touching California.",
      },
      {
        icon: 'Server',
        label: 'Origin Fetch (cache miss)',
        simple: "If Tokyo doesn't have a copy, it fetches it from your California server — just this once, on behalf of everyone in Tokyo.",
        deep: "Edge-to-origin request goes over HTTPS. Cloudflare maintains persistent connections to your origin (connection pooling). Request coalescing: if 1,000 Tokyo users request the same uncached URL simultaneously, Cloudflare sends only one request to your origin and fans the response out to all 1,000.",
      },
      {
        icon: 'CloudLightning',
        label: 'Edge Cache Stored',
        simple: "The response from California is saved in Tokyo. Every subsequent Tokyo user gets it instantly.",
        deep: "Cached with TTL from Cache-Control: max-age or s-maxage. stale-while-revalidate allows serving stale content while fetching a fresh copy in the background. Surrogate-Key headers let you purge groups of related assets with a single API call.",
      },
      {
        icon: 'ArrowLeft',
        label: 'Response to User',
        simple: "The user in Tokyo gets the page from Tokyo in under 20ms — as if your server was right next door.",
        deep: "TTFB from a cache hit: typically 5–15ms. Serving over the existing TLS connection (no new handshake). Content-Encoding: gzip or br applied at the edge. Brotli is 20% smaller than gzip but requires HTTPS — Cloudflare enables it automatically.",
      },
    ],
    failures: [
      {
        name: 'Stale Cache Serving Old Content',
        cause: 'CDN cache was not invalidated after a deploy. Cache-Control max-age set too high (e.g., 86400) for frequently-updated content.',
        symptom: 'Users see old version of site after a deploy. Hard reload (Ctrl+Shift+R) sometimes shows new version by bypassing browser cache but CDN still serves old content.',
        fix: 'Purge cache after every deploy via CDN API. For static assets, use content-hashed filenames (bundle.abc123.js) so each version has a unique URL — enabling permanent caching without stale risk.',
        severity: 'high',
      },
      {
        name: 'Origin Overload on Cache Miss Storm',
        cause: 'CDN cache was fully cleared (e.g., after a cache purge or CDN failover) during a traffic spike. Every concurrent request becomes a cache miss and hits origin simultaneously.',
        symptom: 'Origin server overwhelmed immediately after a cache purge. 500 errors and slow responses. Load spike is 10–100x normal because all CDN PoPs worldwide hit origin at once.',
        fix: "Enable Cloudflare Shield (request coalescing at the CDN level). Use stale-while-revalidate to serve stale content while refreshing. Implement origin rate limiting. Don't purge all cache during peak hours.",
        severity: 'critical',
      },
      {
        name: 'CDN Bypassed by Direct IP',
        cause: "Attackers discover the origin server's real IP (via DNS history, SSL cert logs, or email headers) and attack it directly, bypassing CDN DDoS protection.",
        symptom: 'DDoS hits origin directly. CDN firewall rules and WAF have no effect. Origin overwhelmed despite CDN being operational.',
        fix: "Only allow inbound traffic from Cloudflare's published IP ranges at your firewall or security group. Use Cloudflare Authenticated Origin Pulls (mTLS: Cloudflare presents a client cert to your origin, rejecting requests without it).",
        severity: 'high',
      },
    ],
    commands: [
      {
        label: 'Check if response came from CDN cache',
        cmd: "curl -I https://yoursite.com | grep -i 'cf-cache-status\\|x-cache'",
        what: "Shows Cloudflare's cache status (HIT/MISS/EXPIRED/BYPASS) or AWS CloudFront's X-Cache header. Instant verification that CDN caching is working for a given URL.",
      },
      {
        label: 'Test latency from your location',
        cmd: "curl -o /dev/null -s -w 'TTFB: %{time_starttransfer}s\\n' https://yoursite.com",
        what: "Measures time to first byte from your current location. A HIT should be under 50ms. A MISS will be 150–500ms depending on origin distance.",
      },
      {
        label: 'Find real origin IP',
        cmd: 'dig +short yoursite.com',
        what: "Shows the IP your DNS resolves to. If behind Cloudflare, this returns a Cloudflare anycast IP (104.x.x.x or 172.x.x.x), not your server's IP — confirming the CDN is active.",
      },
    ],
    a: {
      v: '📚🌍🏪',
      t: 'The global bookshop network',
      tx: "Imagine you run a bookshop with one warehouse in New York. Customers worldwide want your books. Shipping from New York to Japan takes two weeks. So instead, you print copies of your bestsellers and stock them in 300 local bookshops around the world. A customer in Tokyo doesn't wait for New York — they walk to their local Tokyo bookshop. CDNs do exactly this for websites: Cloudflare copies your CSS, JavaScript, images, and sometimes entire HTML pages to 300+ cities worldwide. Users get served from whichever city is closest to them, often in single-digit milliseconds.",
      s: "The counterintuitive insight: a CDN cache miss is only expensive for the first user. Every subsequent user in that city gets the cached response — so a CDN is most valuable for popular content at scale, and almost worthless for unique, personalized, or highly dynamic pages. Design your caching strategy accordingly: long TTLs for static assets (JS, CSS, images — use content hashing for safe immutable caching), pass-through or short TTLs for HTML and API responses.",
    },
    te: {
      def: 'A CDN (Content Delivery Network) is a globally distributed network of edge servers that cache and serve content from locations geographically close to users, reducing latency, offloading origin servers, and providing DDoS protection.',
      types: [
        { n: 'Cloudflare', d: '330+ PoPs. Integrated DDoS protection, WAF, Workers edge compute, and Zero Trust network access. Largest network by traffic volume.' },
        { n: 'AWS CloudFront', d: 'Tightly integrated with AWS. S3 and ALB as origins. Lambda@Edge for request/response compute at the edge. Pay-per-request pricing.' },
        { n: 'Fastly', d: 'Highly programmable CDN with Varnish-based VCL and Compute@Edge. Instant purge API (<150ms global). Used by GitHub, Spotify, The Guardian.' },
      ],
      when: 'Use a CDN for any site with global users or high traffic. Even for sites with all users in one country, a CDN reduces origin load and provides DDoS protection. The origin cost savings often pay for the CDN subscription.',
      trade: 'Cache invalidation complexity: stale content after deploys requires a cache purge strategy. Content-hashed filenames solve this for static assets. Dynamic HTML requires either short TTLs or explicit purge on change.',
      rw: {
        ex: ["Netflix Open Connect — dedicated CDN embedded inside ISP networks worldwide", "Cloudflare serves ~20% of all internet HTTP traffic", "GitHub Pages served via Fastly CDN"],
        cs: "Netflix's Open Connect program goes beyond a typical CDN: Netflix physically places servers inside ISP data centers and internet exchanges worldwide. A Netflix subscriber in London may stream from a server in the same building as their ISP's equipment — resulting in single-digit millisecond latency and offloading up to 95% of Netflix traffic from the public internet.",
      },
    },
    interview: {
      q: 'How would you design a globally fast content delivery system for a site with 100M daily users?',
      a: "Use a CDN like Cloudflare as the primary layer. DNS points to CDN, which serves cached content from the nearest PoP. Split content strategy: static assets (JS, CSS, images) get content-hashed filenames and Cache-Control: max-age=31536000, immutable — cached forever, new deploys get new URLs. HTML pages get short TTLs (60s) or pass-through with stale-while-revalidate. API responses are not cached at CDN level (authenticated, personalized). Origin is protected behind CDN — only Cloudflare IPs can reach it. Multiple origin regions (US, EU, APAC) reduce cache miss latency. Purge API called on every deploy to invalidate HTML cache.",
      fu: [
        'What is the difference between a CDN and a reverse proxy?',
        'How do you handle CDN cache invalidation on deploys?',
        'What is cache stampede and how do you prevent it?',
        'When would you NOT use a CDN?',
      ],
    },
  },

  {
    id: 'debug-network',
    cat: 'networking',
    color: '#6272d4',
    icon: '-',
    title: "The site is down. Where do you even start?",
    tag: "A detective's playbook for every layer of the stack",
    hook: "'It's not loading.' Three words that can ruin a morning. No error message — just a blank page, a spinner, or a browser error that tells you nothing useful. The problem could be DNS, TCP, TLS, the proxy, your app, or the database — on any of three continents, possibly in infrastructure you don't directly control. Most people panic and start randomly restarting things. Senior engineers run three commands and know exactly which layer failed in under a minute. The difference is a mental model: the network stack is a chain, and you test each link starting from the outside.",
    simpleExplain: "When your car won't start, you don't immediately assume the engine is blown. You check from the outside in: Is there fuel? Is the battery dead? Does it turn over? Is it in park? You start with the simplest, most accessible checks first — not because they're most likely, but because you can rule them out in seconds. Network debugging is identical. Start at the outermost layer (can I reach it at all?) and work inward toward the application. Each layer you rule out cuts the problem space in half.",
    howItWorks: "The debugging playbook follows the network stack from L3 to L7. First: can you reach the IP at all (ping, traceroute)? If yes: does DNS resolve to the right IP (dig)? If yes: can you open a TCP connection on the right port (nc -zv)? If yes: does TLS succeed (openssl s_client)? If yes: does the proxy respond correctly (curl -v)? If yes: does the app respond on its local port (curl localhost:3000)? If yes: can the app reach the database (connect with psql/mysql)? At each step, a failure tells you exactly which layer to investigate. A failure at TLS means you don't need to check the database. A failure at DNS means you don't need to check TCP.",
    pipeline: [
      {
        icon: 'Wifi',
        label: 'Can you ping?',
        simple: "Send a tiny 'are you there?' message — the most basic connectivity test that exists.",
        deep: "ping ip-address sends ICMP echo requests. Success means L3 (IP routing) is working. Failure means firewall, routing issue, or server completely down. Caveat: many production servers block ICMP for security — ping failure alone doesn't confirm the server is dead. Always follow up with nc or curl.",
      },
      {
        icon: 'Globe',
        label: 'Is DNS resolving?',
        simple: "Does the domain name translate to the IP address you expect? Wrong answer here means nobody can find your server.",
        deep: "`dig +short domain.com` returns the IP your resolver sees. Compare to the expected IP. Empty response means DNS record is missing entirely. Wrong IP means DNS propagation delay or misconfigured record. `dig @8.8.8.8 domain.com` bypasses your local resolver to test from an independent source.",
      },
      {
        icon: 'Plug',
        label: 'Does TCP connect?',
        simple: "Can you knock on the server's door and have someone answer? If the door is blocked, nothing else matters.",
        deep: "`nc -zv host 443` attempts a TCP connection. Connection succeeded means port is open. Connection refused means server is up but nothing listening on that port. Hangs then times out means firewall is silently dropping packets — worse than refused and harder to debug.",
      },
      {
        icon: 'Lock',
        label: 'Is TLS valid?',
        simple: "Is the security certificate real, not expired, and for the right domain? A bad cert blocks all users completely.",
        deep: "`openssl s_client -connect host:443 -servername host`. Look for 'Verify return code: 0 (ok)'. Common failures: certificate expired (check dates), hostname mismatch (check SAN field), self-signed cert (check issuer), missing intermediate CA (chain is incomplete).",
      },
      {
        icon: 'Shield',
        label: 'Does the proxy respond?',
        simple: "Is Nginx or the load balancer receiving requests and handling them correctly?",
        deep: "`curl -v https://host` shows the full HTTP response including all headers and status code. 502 means upstream app is down. 504 means upstream app is too slow. 301/302 loop means redirect misconfiguration. Check `/var/log/nginx/error.log` for proxy-level errors.",
      },
      {
        icon: 'Server',
        label: 'Is the app responding?',
        simple: "Bypass the proxy and talk directly to your app. If this works but the proxy fails, the proxy config is the problem.",
        deep: "`curl http://localhost:3000/health` tests the app server directly without going through Nginx. If this works but Nginx returns 502, the proxy_pass configuration is wrong or pointing to the wrong port. Check app logs: `pm2 logs`, `journalctl -u your-service -n 50`, `docker logs container-name`.",
      },
      {
        icon: 'Database',
        label: 'Is the DB accessible?',
        simple: "Can your app actually connect to and query the database? A DB connection failure causes 500 errors that look like app crashes.",
        deep: "Connect directly: `psql $DATABASE_URL` or `mysql -h host -u user -p`. Check for connection pool exhaustion: too many app instances competing for a limited pool of DB connections causes queuing and timeouts. Run `SHOW PROCESSLIST` (MySQL) or check pg_stat_activity (Postgres) for long-running queries blocking others.",
      },
    ],
    failures: [
      {
        name: 'Works Locally, Broken in Production',
        cause: 'Missing environment variable, different configuration between environments, or a dependency not installed in production.',
        symptom: "Works perfectly on your machine. 500 error or blank page in production. App startup logs may show the crash reason.",
        fix: 'Compare environment variables: `printenv | sort` on both environments. Check that all secrets and config vars are set in production. Look at app startup logs — a missing DATABASE_URL or API key often causes a crash on initialization, before any requests are served.',
        severity: 'high',
      },
      {
        name: 'Intermittent Failures Under Load',
        cause: 'Connection pool exhaustion, unhealthy instance in load balancer rotation, or race condition in application code.',
        symptom: 'Works fine for one user, fails for around 5% of requests under 100 concurrent users. Errors are not consistent — refreshing sometimes works.',
        fix: 'Check load balancer health check logs for instances being marked unhealthy and removed from rotation. Check connection pool stats (pool size vs active connections). Use distributed tracing (Datadog APM, Jaeger) to correlate which specific instances or code paths fail.',
        severity: 'high',
      },
      {
        name: 'Slow but Not Down',
        cause: 'Database N+1 queries, missing index on a table that grew large, or a slow external API call in the request path.',
        symptom: "Page loads in 8 seconds. Users complain but the site doesn't return errors. TTFB is 6–7s. Everything else seems normal.",
        fix: "Check TTFB with curl timing (`%{time_starttransfer}`). If TTFB is slow, the problem is server-side. Enable DB query logging and look for repeated similar queries (N+1 pattern). Check pg_stat_statements for the slowest queries. Add an APM tool (New Relic, Datadog) to see a trace of where time is spent per request.",
        severity: 'medium',
      },
    ],
    commands: [
      {
        label: 'Full connectivity test',
        cmd: "curl -o /dev/null -s -w 'DNS: %{time_namelookup}s | TCP: %{time_connect}s | TLS: %{time_appconnect}s | TTFB: %{time_starttransfer}s | Total: %{time_total}s\\n' https://yoursite.com",
        what: 'One command that times every layer. If TLS time equals TCP time, TLS failed. If TTFB is 3s but TLS is fast, the server is slow. Instant layer-by-layer triage from a single command.',
      },
      {
        label: 'Check DNS from fresh resolver',
        cmd: 'dig @1.1.1.1 +short yourdomain.com',
        what: "Bypasses your local cache and ISP resolver. Tests DNS at Cloudflare's public resolver. Compare output with `dig @8.8.8.8 yourdomain.com` to detect resolver-specific issues or propagation differences.",
      },
      {
        label: 'Test TCP port connectivity',
        cmd: "nc -zv yourhost.com 443 && echo 'Port open' || echo 'Port closed/filtered'",
        what: "Immediate test of TCP connectivity on a specific port. Distinguishes 'server unreachable' (times out) from 'server up but port blocked' (connection refused) from 'working' (immediate success).",
      },
      {
        label: 'Watch error logs live',
        cmd: "tail -f /var/log/nginx/error.log | grep --line-buffered -E '(error|crit|emerg)'",
        what: 'Streams critical Nginx errors in real time, filtered to error severity and above. Useful during an active incident to watch the proxy layer without drowning in access log noise.',
      },
    ],
    a: {
      v: '🚗🔧🔍',
      t: "The car won't start — check from the outside in",
      tx: "When your car won't start, you don't immediately assume the engine is blown. You check from the outside in: Is there fuel? Is the battery dead? Does it turn over? Is it in park? You start with the simplest, most accessible checks first — not because they're most likely, but because you can rule them out in seconds. Network debugging is identical. Start at the outermost layer (can I reach it at all?) and work inward toward the application. Each layer you rule out cuts the problem space in half.",
      s: "The professional mindset: resist the urge to restart things randomly. Every random restart destroys evidence — log entries, error messages, metrics that would have shown the root cause. Instead, gather data first: what does curl say? what do the logs show? what changed in the last hour? Restarts can mask the symptom while the underlying bug remains. Fix the root cause first, restart as a last resort.",
    },
    te: {
      def: "Network debugging is the systematic process of isolating failures by testing each layer of the network stack — DNS, TCP, TLS, proxy, application, database — from outermost to innermost, ruling out each layer before moving deeper.",
      types: [
        { n: 'Black-box testing', d: 'Testing from the outside without app access: curl, dig, nc, openssl, ping, traceroute. Always the first step — establishes which layer is failing.' },
        { n: 'Log analysis', d: 'Reading structured logs from Nginx, the app server, and DB to find error patterns, slow queries, and crash stack traces.' },
        { n: 'APM / Tracing', d: 'Distributed tracing (Datadog APM, Jaeger, Honeycomb) correlates requests across multiple services to show exactly where time is spent per request.' },
      ],
      when: 'Every production incident. Also invaluable for performance investigations — the same methodology applies to slow sites as to down sites.',
      trade: 'Speed of diagnosis vs. depth of investigation: black-box tests are fast but only show symptoms. Log analysis and APM show root causes but require access and setup. Invest in APM before you need it — debugging a production incident without tracing is painful.',
      rw: {
        ex: ['Datadog APM — distributed tracing with auto-instrumentation for most languages', 'PagerDuty — on-call alerting that routes incidents to the right team', 'statuspage.io — customer-facing incident communication'],
        cs: "During the 2021 Facebook outage, internal engineers couldn't access their own debugging tools remotely — because the BGP route withdrawal that caused the outage also took down their corporate VPN. The 6-hour outage required on-site physical access to fix. The lesson: your monitoring, alerting, and debugging tools must not share the same failure domain as your product. Your status page should be hosted on a different cloud provider from your main service.",
      },
    },
    interview: {
      q: "Production is down. Users can't access the site. Walk me through how you triage this in the first 5 minutes.",
      a: "First 60 seconds: run `curl -o /dev/null -s -w 'DNS:%{time_namelookup} TCP:%{time_connect} TLS:%{time_appconnect} TTFB:%{time_starttransfer}' https://site.com` — this tells me which layer failed. In parallel, check the status dashboard and recent deploys (did someone push code in the last 10 minutes?). If TTFB is 0, there's a server-level failure. Check ping, then nc -zv host 443, then curl -v. If DNS returns wrong IP — DNS issue. If TCP times out — firewall or server down. If TLS fails — cert issue. If proxy returns 502 — app crashed (check app logs and process list). If app returns 500 — application error (check app logs for stack trace). If app is slow — database (check slow query log and pg_stat_activity). At every step, check what changed recently — that's almost always the root cause.",
      fu: [
        'How do you set up monitoring to catch issues before users report them?',
        'What is a postmortem and what should it contain?',
        'How do you debug a memory leak in a Node.js service?',
        'What metrics would you put on a dashboard for a web service?',
      ],
    },
  },
];
